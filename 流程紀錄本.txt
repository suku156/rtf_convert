// 目標 : 使用 C++ 實作 wordpad(.rtf 型態) 轉換成 .txt檔

// 解果測試方法 : 嘗試將原來無法開啟的 C++ 筆記轉換成 可開啟的 txt檔

// 使用的開發環境 : VS Code ;  編譯器 : g++

// 第一步 : 處理 .rtf 格式  結果:複製並理解部分 簡易版 RTF 清洗器

// 第二步 : 將測試的轉換 .rtf檔,完善化

   1. 將 字型那行清空 (完成)

   2. 讓 三個特殊符號 ~ - _ 正常保留 (完成)

// 第三步 完成  RTF 中的十六進位表示法(完成)

// 第四步 嘗試處裡 .rtf中的圖檔

  // 因為使用 getline() 不好處理圖檔,所以先讀取整個檔案,將圖檔的部分處理完,在移除後,在按照原本的方式去處理文字檔

  // 進度紀錄 : 已成功拆分次測試用.rtf檔案,.txt檔成功 但是拆解出來的png檔無法開啟 2025/07/24

// 第五步 已經成功 將測試用的 .rtf檔案 分拆成 txt檔與png檔 並且都順利開啟,也都正確顯示內容了 2025/07/28

// 第六步 嘗試使用原有的檔案測試效果
  
   // 結果在原本的.rtf檔案中的圖檔格式 非純hxd檔案,需要更改正規表示法的判斷範圍

      // 2025/07/29 發現原本的檔案中的圖片是使用 WMF(Windows Metafile Format)格式 與原本不相容,需進行額外之處理

// 2025/07/29 臨時狀況在實際轉換文字的時候發現,無法正確轉換 encoded字元需重新處理

  // 成功轉換出中文字,但是原本的行號與空格等格式消失導致不易閱讀需進行調整

  // 嘗試保留原本的特殊字體(換行,空白等) 2025/07/29 成功加入原有的行號以及前方空格 已經可以初步可看出原來架構

// 開始嘗試處理 WMF 型態的圖片

// 2025/08/01 先嘗試使用較為 簡單的 .rtf檔進行修整與測試

  // 修改結果 1 : 順利轉換完成,但是 .txt檔案沒有呈現正確內容,.wmf檔案一片空白

  // 依據結果的嘗試 : 開始測試每一段拆解出來的結果是否有問題

  // 嘗試的發現 : 使用 C++ regex的正規表示法去抓取資料無法很完整的抓取 , 開始嘗試融合其他辦法

// 2025/08/04 已經將函式改用字串搜尋的方法,並以成功將測試用的.rtf中的圖檔切割出來

  // 將切割出來的圖檔範圍內容從原本的檔案中清除(已完成,單一圖片版本)
  // 已經確認處理刪除圖片區塊後的純文字部份已經ok
  // 嘗試接切割出來的圖片檔區塊,順利轉化為 wmf 圖檔

     // 1. 經過詢問 要先執行兩個步驟 1. 刪除資訊中的前導控制資訊 2. 清除剩下資訊間的 空格與換行

// 2025/08/05 嘗試使用 getline() 等函式清除不需要的資訊

   // 1. 已經提取出乾淨的圖片區塊,開始嘗試轉化圖片步驟
   // 2. 已經順利轉成圖片,但是圖片無法正確開啟

// 2025/08/06 查詢過後 決定改用課程中的某一個圖片進行實測

  // 已經成功將目標圖片轉化成功

  // 新目標 : 依據實際狀況對原程式碼進行調整 

     // 目標一 : 在切割完圖檔區域後順便將切割的區域清除乾淨(完成)

     // 目標二 : 依據實際圖片情況進行搜尋條件調整(完成)

     // 目標三 : 完成文字與單一圖片整合的處理(完成)

     // 目標四 : 在圖片區塊留下標示註明是圖片幾(完成)

// 2025/08/07 嘗試對有複數圖片的檔案進行轉換測試

  // 目標一 : 處理複數圖檔(完成)

  // 目標二 : 保留文字處立中的中.英文字(完成)

  // 目標三 : 保留原本文字部分的段落與換行等(移轉到目標四)

  // 目標四 : 依據轉換前後的差異去調整文字處理的函式(只解決掉過多空白行問題,中文字轉換問題未解)

// 2025/08/08 改為對較短的純文字目標檔進行實驗 

  // 已經確認主要問題出在 16進制的中文字轉換上 

  // 其餘部份已先調整剩餘中文轉換問題 

  // 查詢後發現主要問題是要解決 正確從 Big5 編碼轉換為 UTF-8 

// 2025/08/11 嘗試使用windows專用的 MultiByteToWideChar 進行編碼轉換

  // 以成功轉化出正常中文字,但仍需解決 段落問題以及文字的完整性問題

  // 以成功保留範例英文部分 以及保留整句話,目前需要解決順序性問題

  // 為了保有文字的正確順序將按照建議的將整句話都轉化為 UTF-8 而非只轉換中文字部份

// 2025/08/11 成功完整且可讀的將文字部份處理乾淨

// 2025/08/12 開始嘗試對較為複雜的目標開始測試

   // 目前測試發現 : 純文字檔目標 --> 可行具備可讀性   含有圖片檔的目標 --> 圖片沒有問題但是文字部份會變成亂碼

   // 經過測試發現.將含有圖片的檔案中的文字區塊單獨拆出後可正常轉譯,可懷疑是圖片轉換後遺留的文字訊息導致亂碼

   // 經過詢問發現可能是在取代圖片時,留下的標記文字破壞掉了檔案的整體結構導致的亂碼

   // 嘗試建議解法 : 先記錄圖片區塊位置，最後再批次替換

   // 嘗試解法結果 : 依舊是亂碼,但是在測試中證實了主要問題是出現在插入文字時造成的,主要問題很可能是結構破壞導致的

   // 改為刪除掉以處理的圖片區塊後已經可以正常轉譯,開始測試其餘檔案

   // 新問題 無法處理復數圖片 (以解決,問題出在給予erase()的參數給錯了)

   // 已經確認可以轉換複數圖片,以及可以將文字轉化為正確可讀的版本

   // 新目標: 等待轉換完文字後再插入文字標記

   // 詢問過後被推薦的新方法 : 核心方法 —「位置記錄」+「占位符替換」(透過**占位符（placeholder）**技巧實現。)

// 2025/08/13 嘗試改用純英文加上數字的標記 測試會不會導致亂碼

  // 測試成功 改為純英文加上數字結構後,不會破壞整體結構導致轉譯出亂碼的問題

// 2025/08/14 基本要求已經達成開始往更完整的方向前進

  // 成功將開啟的目標檔案名稱由固定名稱改為輸入的名稱

  // 嘗試使用 C++17 以後的 std::filesystem 來擷取路徑中的檔名來當作,輸出檔案的名稱

  // 在將名稱轉換為 UTF-8 以後,已經成功攝取名稱來當作輸出檔案的名稱

  // 將隨機抓取一個目標檔案來測試效果

  // 新問題 : 過於複雜的檔案名稱會導致開啟檔案失敗

// 2025/08/15 新問題的根本原因是 名稱太複雜會有不支援的符號導致亂碼

  // 詢問出來的解法 : 手動把 本地編碼轉換成 std::wstring

  // 因為只使用main 在遇到 中文檔名時有亂碼問題 因此嘗試使用 wmain()

  // 在將字串改為寬字串的過程中,原本的程式碼出現許多問題需要一一修改

  // 經過詢問後的建議是 檔案的內容處理全都用 std::string  只有在檔案路徑以及輸出顯示上使用 std::wstring

  // 編譯一直跳出錯誤訊息 , 將從使用 wmain() 跳回去使用 main()

  // 已經解決編譯器找不到執行點的問題,新問題是無法解讀輸入訊息,將嘗試使用 std::locale 進行轉換

  // 新的問題 原本轉換的方式已經在 C++17 以後被棄用 需要使用新的方法

// 2025/08/18 目前卡在想要對中文檔名有動作,需要將 int main() 改成 int wmain() 但在編譯及執行程式碼上遇到很多難題

// 2025/08/19 新建了一個 int wmain() 的簡短測試碼 開始嘗試發現問題

  // 測試 1 : 加入 std::cout 看看,結果:cout 正常顯示,wcout 沒有 , 可能問題:Unicode 輸出問題

  // 詢問gpt結果 :  // 讓 wcout 用 UTF-16 模式輸出 (Windows 控制台能正確吃到) 程式碼中加入(_setmode(_fileno(stdout), _O_U16TEXT);)

  // 測試結果 : std::wcout 已經 ok 但是換std::cout 不見了

  // 詢問gpt結果 : 此為 Windows CRT 的限制

  // 解法 : 1.全用 std::wcout,2.啟用分流系統,3.採取切換的方式 , 先使用 方法1:進行嘗試

  // 已經成功讓原本的程式碼使用 std::wcout 顯示訊息,但是程式卻沒有執行動作

  // 經過排查發現.執行到開啟輸入檔案有問題沒能執行,並且錯誤訊息也沒有顯示出來

  // 錯誤訊息找的是 cerr 分流,之前只有設定執行 cout 用UTF-16 現在把兩分流都強制設定

     // 在主程式最前方強制設定

        _setmode(_fileno(stdout), _O_U16TEXT);
        _setmode(_fileno(stderr), _O_U16TEXT);

  // 檔案無法順利開啟是因為 std::string 用中文會導致亂碼,因此不要傳 std::string 改直接傳 std::filesystem::path


  // 已經成功完成檔案處理,轉換中文檔名成功,圖片轉換成功˙新問題原本除了圖片外的內容不見了

  // 詢問gpt過後決定開始嘗試 wstring 與 utf8 之間尋找一個穩定轉換的好方法

  // 先嘗試使用 C++20 <locale> + std::mbstate_t + std::codecvt 方案 此方法有跨平台的優勢可以先進行一波嘗試

  // 成功將原本的字串轉換成可閱讀且排序好的文字,文字轉換ok,圖片轉換ok,保留正確檔名ok,以達成初步要求

// 2025/08/25 以達到基本目標將開始進行下一步的完善

  // 目標 : 將產生產生出來的目標物放入指定名稱的資料夾中好進行正確的分類

  // 第一步 : 先嘗試產生一個正確名稱的資料夾(fs = std::filesystem)

     // 使用  fs::path outputDir = inputPath.parent_path() / baseName; 的方法獲得正確的路徑

     // 在使用 fs::create_directories(outputDir); 創造出正確名稱的資料夾

  // 第二步 : 嘗試將產生出來的資料正確的放入資料夾中

     // 先嘗試將 txt檔案放入

     // 已經成功將 txt檔案放入指定的資料夾中,且輸出的文字資料也正常

     // 開始嘗試將轉換的圖片檔放入 指定的資料夾中

     // 計畫進行較大的改動,將圖片處理的所有函式統整成一個類別(class),以達到更多的效果

     // 目前思路 : 保留最一開始的圖片切割函式,在此函式中在呼叫類別的功能來達成目標

        // 1.在找到資料夾路徑時,順便存入類別中

        // 2.在使用圖片切割函示時額外傳入類別來使用

        // 大概的方向 : 將圖片處理所需的資料寫成類別成員,剩下的轉換所需函式則寫成類別函式

// 2025/08/26 嘗試完成類別工具的使用

  // 1. 將原本複製的字串改為複製到類別中的函示成員

  // 2. 嘗試改為使用移動避免資源損耗(編譯成功)

  // 3. 在嘗試將函式類別工具化時,發現原本的函式方法具有順序性,為了保持此順序性.嘗試加入使用 'enum 的狀態機' 的機制

  // 4. 已經成功將函式的功能轉移成類別成員函式具備的功能

  // 5. 已經成功將原有的圖片轉換功能包進類別中,將繼續嘗試將產生圖片放進同一個資料夾中

  // 6. 已經成功將產生的檔案放進正確的資料夾中.階段性目標達成 

// 2025/08/27 新的目標一次處理一個目標資料夾的檔案內容

  // 為了新的目標(對整個資料夾使用),我需要把原本的main()的內容 包進一個新的類別中

// 2025/08/28 嘗試將原本的主程式功能封裝進新類別中

  // 成功將原程式碼放入新類別中,並且成功對單一目標檔案使用並正確執行,接下來將嘗試對資料夾目標實作

  // 成功將原有程式分為對單一檔案執行,以及對整個資料夾進行動作

// 2025/09/02 新的目標 讓程式處理更健壯，輸出結果更穩定

  // 經過查詢發現目前處理方式只適用於台灣環境,加上偵查功能可適配更複雜的環境(EX:簡中 GBK、日文 Shift-JIS、歐美 CP1252 的 RTF)

  // 1. : 偵測檔案 ,1.確認是否為.rtf檔 2.確認檔案的基準編碼宣告

  // 2. : 選擇回傳值 1.int 2. enum (選2)

  // 3. : enum放哪?

  // 4. : 決定新增一個類別,專門用來負責 偵測的所有事項 並將enum 放入此類別中

// 2025/09/03 開始類別創作

   // 成功建立最簡單的檢查是否為 .rtf檔 函式,需改進先建立資料夾之行為

   // 以調整順序,會先進行檢查正確後才會繼續執行

   // 合併為單一成員函式,如果不是目標格式會回傳非正確格式的 enum

   // 已完成最基礎的偵測,將開始下一步 確認 ansi 格式

   // 嘗試擷取ansi 編碼值

   // 已經新增常見編碼選項

// 2025/09/04 先測試是否能正確偵測編碼選項,在看看能用其做什麼

   // 已確認能夠依據正確的標頭編碼進行回傳

   // 因為處理文字目前是用函式包函式的方法不好傳遞資訊,因此要將函式包成一個類別再用類別成員儲存資訊

   // 已成功包成類別

   // 開始嘗試將得到的資訊放入函式中

   // 使用成員函式儲存資料,將改成用資料轉換而非 預設 950

   // 已經修改完畢,詢問額度已滿 , 將等待一段間後 開始測試轉碼效果

// 2025/09/05 開始尋找測試轉碼效果成效的方法

   // 使用Gpt 直接給的測試檔,轉譯失敗嘗試其餘測試檔

   // 測試原本成功的檔案,改變Code 碼 之後全部變為亂碼

   // 亂碼原因是因為我多了一個 hex轉big5 的動作這樣會只對繁中起作用

   // 將嘗試進行分流 950(繁中) -> 使用原路線 其餘(直接依照邊碼丟給轉換器)

   // 已加入分流,依舊需要找到檔案已知道各分流需要什麼加工

// 2025/09/08 到 Github 以及 Stack Overflow 沒有找到所需的檔案,將開始嘗試自行製造

  // AI 給的測試檔效果很差,將嘗試使用 word 或其 替代品產生測試用rtf檔

  // 查詢了一下 將嘗試使用 LibreOffice Writer

  // 因存檔方式不同將嘗試手動加入測試頭檔

  // 經由實測發現不能直接轉碼 依舊需要經過一個類似 DecodeHexToBIG5 的步驟之後再轉 才會有效果,不然依舊是 hex 碼而已

  // 須找出不同編碼的轉譯條件已達到通用的效果

// 2025/09/09 發現不同ansi編碼間的差異,需要依據不同的編碼決定,hex碼轉換的方式

  // 將流程改成 依照將 hex碼 -> byte 流, 在將byte流依據 code碼做轉換

  // 只有 950 成功轉換 , 其餘編碼依舊是亂碼

  // 先嘗試針對 936 進行改造

  // 詢問過後發現可能的原因是 擁有的 byte陣列不夠乾淨

  // 發現將轉換hex碼 的函式的回傳值改為 std::vector<uint8_t> 會比 std::string 好

// 2025/09/10 嘗試改造轉換用的兩個函式,使其不再侷限於 繁中(950)

  // 修改函式結果 , 依舊是亂碼

  // 發現新問題,測試用的檔案是混合的,其真實的資料存在(un中) ,屬於下一階段的目標,因找到符合目前目標的測試檔案

  // 查詢過後發現 : 現在RTF 檔案都已經改用 UTF 舊資料才用 ansi碼,

  // 新計畫 : 開始偵測RTF檔案邊碼 , ANSI -> 使用舊分流 UTF -> 先分出來之後再進行後續的處理

  // 突然拿到檔案, 以確認 存ansi碼可依據 code碼進行編譯,下一步驟:分清楚檔案格式

　// 得知分辨 UTF 的方式是依據 BOM 的前三個符號,將在偵測中加入此步驟

  // 目前加入 有明確目標的 BOM 檢查, 接下來嘗試分辨 無 BOM 分辨是 UTF-8 還是 ANSI

// 2025/09/24 嘗試以物件的思考方式慢慢整合新功能

  // 新類別先存取檔案前 4 bytes 以此來判斷

  // 以建構子存取需要檢查之資料

  // 以成員函式 回傳之 enum 來進行第一步判斷  

  // 在成功讀取檔案後,依據函式回傳之 enum 來決定下一步驟 

  // 下一步驟 :　如果沒有　ＢＯＭ　或是　無法判斷的話就使用後續函式處理

  // 新的想法 : 將 偵測分別寫成三種類分別去處理 : 有 Bom , 無Bom UTF-8 ,ansi

// 2025/10/01 想嘗試先判斷 有BOM -> 是否是 ANSI -> 都不是就假設是無BOM　ＵＴＦ－８

　// 開始實作　： 先創造一個共用的基底類別,然後把所有有可能的enum 放到基底類別中

  // 已完成介面實作,開始分別對繼承該介面的類別進行各自的實作

  // 已經完成3個類別中的第一個 偵測有BOM的類別

  // 已經完成3個類別中的第二個 偵測ANSI

  // 臨時調整為了整體程式碼的乾淨與一致性,將會把讀取資料的動作放入都有的 Detect()函式中

  // 已經修改 , 疑問:這樣是否違反 開放封閉原則,是否將讀取任務分割給另一個函式比較好

  // 嘗試新增一個轉換函式放到 protected中 : 如果沒有問題將會嘗試都放入工具中

  // 已經完成3個類別中的第三個 判斷假設為 NOBOM_UTF-8 是否為 錯誤

  // 將會嘗試使用 facade 模式將 三個類別包成一個可以比較簡單使用的類別來使用

// 2025/10/02 嘗試實作出 Facade 模式的類別,並且單獨將幾個類別內去進行測試

  // 已完成整合類別,將開始嘗試測試

  // 簡易測試完成,將進行下一步將 讀取任務 以及 偵測任務分開

  // 已經成功完成切割,開始將新的程式碼覆蓋回去

  // 覆蓋完成,接下來開始實際使用偵測所得出來的結果

  // 已經成功將類別融入原有程式碼中,下一步將開始實際使用得到的資訊去設定如何編譯文字的部份

  // 嘗試新增一個類別,專門用來儲存所有需要用到的資訊

  // 需要改造原有的程式碼,將 enum 獨立出來

  // 已經實作成功專門用來儲存資料用的類別,現在我需要進一步的拿此資料去對文字處理的類別進行新的加工

  // 確認加工流程先進行正確的編碼轉換,再去進行清理控制碼的動作

  // 卡住了 要如何進行自動分類處理暫時沒有頭緒

// 2025/10/03 實作三種編碼處理用的類別,專門用來轉譯

  // 詢問過後發現因該將解碼任務分成兩層,第一層 解碼 輸出乾淨的 UTF-8 第二層在進行解析的動作(清理控制碼等等)

  // 詢問後發現因該要做的流程為 std::ifstream 讀檔->存成 rawBytes -> 依照編碼類型正確處理 -> 處理完得到正確的 UTF-8 std::string 在處理

  // 為了更順暢處理將儲存資料的類別新增函式,以此大致分類是哪種編碼

  // 已經完成初步的執行編碼陣列正確轉換為 std::string的程序,接下來需要將類別一一實作即可
    
  // 以實作處理 UTF-8 的類別(可直接封裝)

  // 開始實作 UTF-16解碼類別

  // 從GPT 那邊複製了一份 處理 16 轉 8 的 函式先用用看

  // 最後嘗試實作 ansi 轉換類別

  // 發現為了通用性(windows/Linuxs都要用的話), 因該使用 iconv 而不要使用 MultiByteToWideChar 

  // 發現自為止使用的編譯器都是獨立的,現在需要新的資料庫 因此要安裝並轉換到 msys2 環境

  // 已經成功將編譯器使用改成 msys2 環境,並且已經成功include  iconv庫 之後就可以開始使用 

// 2025/10/07 實作最後的 ansi 轉 utf-8 類別 , 使用 iconv 庫

  // 初步完成 類別函式並通過編譯(尚未測試)

  // 以完成 codepage 碼 轉換成所需 iconv 之字串 函式

  // 嘗試完成清理控制符以及特殊符號處裡的工作

  // 先進行測試,試試看到目前為止(處理完圖檔) , 的輸出模樣再來決定後續的處理

  // 因為中文亂碼的問題,先將資料夾移到全英文環境

  // 新問題須解決新編譯環境 msys2 的編譯問題

  // 終於找到編譯的問題 : 問題出在 系統預設的PATH(裡面) 沒有 msys2 編譯所需的資訊,因此要在tasks 的設定檔案中給定路徑,才找的到

  // 雖然編譯通過了,但是程式變得無法執行,感覺要處理中文不能用 msys2 環境,因該回去使用 mingw64 在看看能不能額外使用iconv庫才對

// 2025/10/08 將編譯環境改回去原本獨立的 mingw64 

  // 已改回原本的環境,接下來嘗試加入 iconv

  // 以將下載的iconv 加入 , 接下來會先進行 iconv 使用測試

  // 使用 iconv 測試檔沒有跳出錯誤訊息,可移步到原生程式進行測試

  // 已成功導入 iconv 庫 ,開始對原程式進行調整

  // 已經確認有偵測到正確的 codepage 數字

  // 感覺跟使用暫時物件去設置有關

  // 確認與暫時物件使用有關,因此改為嘗試使用物件串聯

  // 輸出又變回 1.2011\'a6\'7e\'ab\'e1\'aa\'baC++\par 這樣,感覺實際解碼流程還有問題在進行一波調整

  // 詢問後發現是只有 ansi 編碼 需要多一個轉換動作,其他不需要(未測試)

  // 已經成功在 ansi 編碼的情況下 順利將 \'XX 的符號傳換為中文 接下來要將 控制符號與特殊符號處理乾淨

  // 已經使用舊有的類別工具處理換行等問題
  
  // 嘗試完善清理函式

  // 發現使用 regex 正規表示法時常常誤刪或沒刪到,將嘗試改用其餘方法處理

  // 第一次清理測試(使用平衡掃描法 藉由 {} 數量來移除) 結果 : 已清理掉部分控制區塊

// 2025/10/09 繼續清理剩餘的純文字區塊

// 2025/10/13 目前只剩下 \cf0\f1\fs44 這些控制碼要清

  // 目前已經可以完全將檔案變成純 txt 檔了

  // 直接對有 文字與圖檔 跟 純文字 兩種檔案的資料夾做測試 , 均可正常轉換,但圖檔遺留文字需進行額外加工處裡

  // 目前剩餘 \pard\sa200\sl240\slmult1 此行需清理乾淨

  // 已經正確的清理乾淨,將所有檔案放到一起以俐落使用

  // 已經經過初步測試 複合型資料夾內的檔案 , 純文字檔與圖檔都正常符合預期,接下來來對 (utf)體系進行動作

  // 使用 libre 建立了一個 utf8 的測試檔案,先從能否正確偵測開始測試

  // 已經確認可以正常的偵測出 是 utf8 的編碼,接下來要開始實作 utf8 的解碼

  // 目前得知解 utf8 編碼 需要 : \uxxxxx (x是數字)  不需要 \'b7 這是 ansi 編碼 由該類別負責

  // 開始實作第一步 清除ansi編碼 

  // 實作了 清理 ansi 編碼的任務,詢問看看有無隱患或其他問題

  // 詢問後發現因該 只需要刪除 \uxxxxx 後面的 \*xx 符號而不是直接全部刪除 , 因為混合使用存在著可能性

  // 修改程序 在字串中找到 \uxxxxx 的部份進行轉譯,在順便把其後方的 ansi 編碼刪除 並且不使用 regex

  // 成功轉譯 un碼 但是清理 ansi邊碼痕跡需要修改

  // 成功轉譯 un碼 並清理掉後方 ansi 編碼痕跡, 現在需要開始清除控制碼

// 2025/10/14 開始嘗試清除UTF8 檔案中的控制符等等

  // 經過詢問 rtf檔案的控制符與其使用何種編碼沒有掛鉤,只與其是 rtf 檔案有關

  // 目前目標 : 完善我的 rtf 控制符清理,讓其可以使用在所有檔案上

  // 目前遇到新檔案會刪光光的問題,將改進原有程式碼

  // 將原有刪除開頭控制碼的方式改為,依照尋找前置群組,依照計算括號數量來確認範圍,藉此將整段群組刪除

  // 已經成功將前置群組及開頭移除

  // 因為 utf8 檔案的 \pard 落控制符明顯多於原有的,所以 pard清理函式也需要升級

// 2025/10/15 繼續完成清理函式的實作

  // 完成 GPT 給的函式了,需要進行修改,因為其沒有刪除只又略過數字而已

  // 修改函式原本的功能使其改為原地刪除,不用回傳位置

  // 成功刪除\pard 段落以及其後方的控制符,現在來處理群組 

  // 嘗試重新實作群組處理的部分

  // 已經成功刪除 全部控制符,也處理掉群組的部份,目前剩下段落跟分行等東西需要進行處理

  // 臨時實驗,目前的改動對舊的 ansi 編碼檔案的清理控制符可以通用

  // 加入段落修正的函式進行測試

  // 段落已經處理成可接受的狀態,現在來處理遺留的獨立大括號問題

  // 已經完成實作接下來進入測試環節

  // 測試時遇到新問題,之前可以正常處理的檔案如果是有圖檔的用完圖檔在處理會變成亂碼

  // 測試更多檔案後發現,有的含圖檔的會亂碼,但是有的沒有這個問題可以觀察這兩種檔案的差異來找出問題

  // 先假設是圖檔處理的問題,發現取消圖檔處理的話確實不會再發生任何亂碼的問題將會重新檢視圖檔處理函式

// 對圖檔處理函式進行修改以避免亂碼的發生

  // 修改搜尋邏輯

  // 修改留下標記

  // 已經修該,開始試試對亂碼檔案有無影響  結果 : 無影響

  // 嘗試調整整個任務的流程,改為先處理完圖片區塊

  // 因為調整順序的關係所以 之後解碼改為吃 std::string

  // 調整完成後處理順序變快了,但是某些檔案的文字檔依舊是亂碼,但是某些原本是亂碼的檔案恢復了,需要更多測試檔案來找出問題

  // 開始進行更多檔案(原有的ansi 未包含utf8檔案)的測試,將找到的有問題檔案放到一起

  // 因為有問題的檔案,的圖檔區塊都是沒有問題的,所以找一個純文字資料夾來進行測試看看

  // 在測試途中第一次出現 :　無法建立輸出檔案的情況

　// 先處理圖檔檔案的特殊情況 , 先改成輸出處理完圖檔還未轉譯的情況

  // 經過測試發現 : 有的檔案是 經過清理控制符與群組才會發生問題,有的是處理完圖檔就有問題

  // 先處理控制符函式的問題

  // 目前最有可能是因為中文編碼的特殊性導致的問題

  // 修改 eraseHeaderControls() 函式 加入防止 中文{} 誤判的機制

  // 原有的 stripPropertyGroup() 函式太過簡易(遇到中文ansi編碼會亂碼),加入多重判斷機制

  // 已完成改進 函式 需要的函式工具 , 正式開始改造

//2025/10/17 將原有函式改進為,不會被中文byte 困惑導致亂碼

  // 修改後依舊會導致亂碼,需要再次調整

  // 發現不使用 regex 後,部份檔案可以免除亂碼

  // 嘗試將原本的清理函式中有用 regex 的改為不要用到 regex 版本

  // 將新的函式整合成使用 std::map 容器使用

  // 實作了不用 std::regex 的處理函式,並依照建議修改使用順序,將進行測試

  // 測試順利文檔有變乾淨且沒有變成亂碼

  // 開始實作不用 std::regex 的最終清理函式,把剩下的控制符全部刪掉

  // 目前剩下 開頭 跟 結尾的 大括號與空格

  // 剩下到好的清理 大括號的方法

// 2025/10/20 嘗試清理最後的大括號之後進行,另一個檔案的特別錯誤處理

  // 實作擷取有效區間的函式

  // 擷取有效區間也容易因為中文編碼問題導致誤刪而亂碼

  // 已經實作完成移除開頭與結尾的大括號

  // 經過測試目前 ansi 編碼檔案都已經可以正常運作

  // 測試 utf8 編碼的檔案

  // 新的發現 \ucN(N 是浮動數字,用來代表後方要忽略 N 個字元) 是RTF 官方規定用來處理的, 目前用不到先將其移除

  // 已經移除 \ucN 控制符 , 接下來處理 英文字與數字的單獨空行問題

  // 嘗試函式失敗,對UTF8 編碼檔案無作用 , ansi 編碼檔案會被影響成亂碼

  // 詢問過後發現此種情況只會出現在 utf系列的編碼中,因此不因該將其放入共通的文字修飾階段而是放進轉譯的類別中

  // 判斷函式沒有作用也許在 轉譯前就先處理會比較好?

  // 將策略改成在轉譯前先找到相關群組(比較好找),並對其進行清理

  // 目前發現目標大多數是像這樣 {\hich\af5\loch\scacaps0\cps0\f8\fs25\expnd0\expndtw0\i0\b0\f5\loch
     Bernard Arnault} , 目前目標 : 1.找到這樣的群組 -> 2.確認裡面是 純英文.數字 -> 3.清掉控制符與大括號 -> 4.替代回去

// 2025/10/21 嘗試實作在控制群組還在階段就將英文與數字群組先處理完成

  // 目前看到的都是以 {\hich 做為開頭先嘗試以此為目標

  // 成功初步解決分段問題,但是造成多餘大括號遺留

  // 先嘗試關掉文字修飾的類別,觀察遺留控制符剩下什麼

  // 逐一測試後發先需要調整 finalRTFccClean(Cleaned) 這個函式

  // 發現不需要調整舊有函式,而是需要新增一個函式先去處理{\loch 開頭的群組

  // 新增函式處理群組後,初步成功將測試更多檔案好確認功能完善度

  // 找了十篇中文文章.測試utf8 檔案處理 , 目前發現單獨的數字標題 或  標點符號 有些會有問題待改善 英文大部份已經沒有問題了

// 2025/10/22 先對比處理後的文章差異,再來看那些部份需要調整

  // 找到的幾個問題 :
  { 
    問題 1 : 每個純文字檔 .開頭都會空六行 , 
    問題 2 : 在 具有 「 」 的某段區塊出現無意義分行 (只出現個別案例,大部份都正常) , 
    問題 3 : 出現文章段落如果以數字(0~9)做開頭,數字會消失的情況(必定發生) , 
    問題 4 : 遇到段落以數字開頭導致出現非預期分行的情況 (數字也會消失)
  }

  // 先處理問題 1

  // 嘗試 : 在文字處理時如果發現開頭是空行就先刪除

  // 成功解決問題 1 ,開頭不在有多餘空行

  // 嘗試調整段落間過多空行問題

  // 已調整過多空行問題

  // 詢問後得知問題 2 是轉譯階段的問題,嘗試調整 utf8 轉譯 

  // 對比處理前後 , 感覺問題不是主轉而是 文字處理階段的判斷問題

  // 需要重新省視一些調整函式因該在轉譯前執行還是轉譯後

  // 轉譯前調整風險過高,因改成轉譯後加上更多條件來調整 

  // 調整 處理 \loch 群組的函式

  // 因該先各自測試目前 文字處理類別的各個函式的作用

  // 發現舊有的 textpardClean 函式無用還會造成誤判

  // 發現主要正文群組分成 {\hich 跟 {\loch 的開頭

  // 問題 {\hich 跟 {\loch 群組只有 utf 語系才有,ansi 編碼是不需要理會這個東西的,因此我不該把處理群組的任務放到共通的 文字處理函式中
   
  // 將處理 utf 體系群組的任務抽離出轉譯類別

  // 實作處理 utf 體系群組中, 發現不同群組的條件不一樣 , 在進行調整

// 2025/10/23 將處理不同群組改為各自的函式

  // 目前的兩個目標 1. 找出為何會多出 {} 跟 其中 ~ 的原因 2.找出以數字做為段落開頭但是數字會消失的原因

  // 先測試只處理群組對 目標1 的影響

  // 修改 loch 群組處理函式,解決目標1

  // 觀察 目標2 原因

  // 發現 hich 群組處理完會跟前方控制符貼著,導致數字被誤判為控制符的一部份而被移除

  // 嘗試如果是數字開頭加入一個空格來避免誤判

  // 觀察刪除邏輯判斷加入空格有沒有用

  // 有用清除邏輯只吃一個空格剛好直接用掉
  
  // 新問題:保護成功但是每個數字都會有空白,解法:改用特別符號之後再一次清除

  // 成功:保留了開頭數字,並且沒有了多餘空格問題

  // 目前剩餘的問題 : 有文章依舊有多餘的大括號與奇怪換行,依據經驗合理懷疑是某個群組的問題

  // 需調整處理 hich群組的函式

  // 目前測試的十個檔案都OK了

  // 回到之前其他有問題的 ansi 編碼的檔案 , 發現跟圖片處理函式有關 

// 2025/10/24 找出圖檔處理函式的問題

  // 強烈懷疑陣列的迴圈判斷導致錯誤

  // 找尋餘檔案圖片應證猜想

  // 已經確認如果圖片檔案是單數使用圖片處理函式就會導致亂碼問題

  // 使用 RTF 規則允許群組保護圖片標籤

  // 加入保護群組機制後已經沒有亂碼了,嘗試在文字修飾階段進行清除

  // 清理後又會變成亂碼,先保留群組好了

// 2025/10/27 找出特別檔案跑出錯誤訊息的原因

  // 依靠排除法懷疑原因出在檔案名稱上,修改名稱探詢原因

  // 移除檔案名稱中的空白進行測試

  // 確定是檔名中的空格問題

  // 新增檔名空格的檢查與替換程序

  // 成功通過新的函式防止檔名中的錯誤空格導致建檔失敗

  // 目前已經將先前測試檔案中的錯誤修改到及格的狀態

  // 嘗試生產最後一種的 utf16編碼 的檔案,進行測試與調整 

  // 產生的檔案無法辨別,需判斷是偵測的問題還是檔案本身的問題

  // 用 Libre 無法順利產生 UTF16 檔案 , 將嘗試實作用 iconv 庫 將utf8 轉換成 utf16

  // 成功轉換出測試檔案,並使用 HxD 確認開頭為 UTF16-LE 開頭

  // 原檔案依舊偵測失敗,證實需要調整原檔案

// 2025/10/28 已經確認辨別 UTF16檔案的類別沒有發揮作用,對其進行修改

  // 透過對資訊流先重置的動作以避免讀錯,已經成功正確辨識

  // 接下來將測試轉譯的動作

  // 發現現在的 utf16Decoder 做的事情實際上是將 utf16 轉成 utf8 

  // 因為實際上做的是 轉碼 而不是 解碼 , 因此其不因該繼承 解碼介面而是因該實作一個新的介面給它

  // 已經將其改為轉碼類別

  // 實際測試其效果

  // 已經依照 UTF 8 時的測試,隨機找了十篇文章,都可正常顯示

  // 目前測試的 都是 UTF16LE 試試看改成另一個 UTF16BE 有沒有效果

  // UTF16LE 與 UTF16BE 都可以正確認出並解碼

  // 進入下一步 特殊符號測試 

  // 先建立一個含有測試內容的 母檔

  // 將 UTF8 編碼的母檔使用 iconv 轉換函式 分別轉換為 ansi(950),utf16LE

  // 透過 iconv庫 轉換函式,已經成功擁有 三種編碼的特殊符號 rtf 檔案

  // 遇到問題 真正 ansi 編碼形成的檔案 無法用 iconv庫 轉換出來,需要使用別的方法

  // 要先搞到可以用來測試的 ansi 編碼的 測試用 rtf檔

  // 已經透過另一台舊筆電用到 純ansi 編碼 之檔案

  // 測試結果 : 都無法正確顯示 , 需要進一步找到原因

  // 先關閉 文字修飾函式 , 看是錯誤移除還是解碼問題 

// 2025/10/29 先觀察是不是解碼方面的問題 

  //先解決 utf 方面 現在看來與群組的處理有所關連 

  //問題一 : 缺乏處理　｛＼ｄｂｃｈ　群組的函式

  //問題二 : 所有群組處理完後都因該在開頭與結尾放入特殊符號避免跟前後的控制符相聯導致誤刪

  //新增 dbch 群組時發現,處理這三個群組好像是在做一樣的事情,好像可以將這三個進行合併的動作

  //合併成功 , 開始對特殊符號群組進行測試

  //遇到的問題在於,該如何知道這段大括號內的事需要的特別群組,還是不需要的控制符號?

  //發現如果是 utf 體系,可以透過數字區間 來判斷是否是特殊符號

  //開始實作 utf 體系的特殊符號判斷類別

  // 問到一個函式可以檢測範圍內是否都為特殊符號

  // 需要對特殊符號再 RTF 中的表現進行更深入的理解,好進行處理

  //觀察結果: 1.單獨存在會有大括號隔開 2.放在文字中間或後方 , 會視為文字的一部份不特別處理 
              3.放在文字的開頭也會有特別括號隔開 4.有emoji 圖示的話 txt檔 會顯示不出來

  //想到的處理方法(utf體系): 檢查每個大括號 {} ,如果裡面是完全由 特殊符號構成 解除 {} 並且在開頭插入'X01' 做保護 , 這樣可以解決觀察1跟3
    , 觀察2 可以不理會群組會直接解決

  //將在處理 utf8 的處理群組類別中,新增特別符號群組處理函式  

  //已經成功在 utf 編碼的rtf檔案中加入特殊符號處理步驟,目前剩下 ansi 編碼的rtf檔案需要做出處理

  //有些特殊符號在 ansi 編碼表中是不存在的,如果有遇到會以特別的\uXXXX? 來特別顯示

  //先處理另一個問題,文字處理函式中有會誤刪開頭左大括號的函式要先找出來

  //需要對 eraseHeaderControls(Cleaned) 函式進行調整避免錯誤刪除開頭的 左大括號

// 2025/10/30 先對開頭清理函式進行修改修正其一直偷刪除開頭大括號

  //函式需要調整為 只對開頭的控制區塊做,以此避免對 ansi 編碼的正文區產生影響

  //使用轉換去做限制效果不好,改成使用範圍去做限制

  //目前直接詢問得到的效果都不好我應該觀察一下兩種編碼檔案的現況,在依據總結來進行任務

  //感覺可以先這樣,先找到開頭的 {\rtf 跳過之後, 遇到的 {\ 開頭的檔案連同內部的蜂巢檔都一起刪除

  //目前改成移除全部 {\ 開頭的群組目前是沒有影響,但預期會影響到圖片標記符的部份

  //圖片標記符的 {\*\ 存在很有必要,要在刪除控制群組中加入判定避免誤刪

  //特殊符號可以了,可是調整卻使得之前的圖片檔出了問題也是文字修飾類別,在觀察一下是誰的影響

  //問題很可能出在再修飾字的時候誤刪導致 byte 間的錯亂

  //需要一個新的保護機制,確保在對文字進行修飾的時候不會導致 誤動到中文的byte 

// 2025/10/31 需要為文字修飾函式新增保護機制確保其不會動到,中文中與控制符號相同的byte

  // 實作了問到的小工具,並在原有函式中加入了,高位元判斷及保護機制

  // 意外發現,很可能所以文字修飾類別的判斷都需要限制在 ASCII 範圍才能更好的避免亂碼

  // gpt 有個建議是減少 erase() 在不同函式中間的頻繁呼叫,容易導致位置的混亂

  // 發現 : 只要加工沒有對 {} 做動作就算沒有做 高位保護也不會發生亂碼

  // 中文亂碼的根源是「誤砍中文字的部分 bytes」 , 也許我不因該再繼續對字元做加工, 而是對 byte

  // 需要重新設計文字修改函式 ,先嘗試能不能從byte 層面安全移除群組之類的

  // 已經先確認過六個檔案都沒有問題,先嘗試修除控制群組

  // 目前實作有考慮位元誤判刪除群組函式沒有問題 , 需加入圖片標記保護

  // 詢問後發現 std::string::compare　是逐byte掃描,只要給的起始位置正確不用擔心 byte誤判

  // 已經完成第一個修改過的函式,在不誤判byte 的情況下,完成控制群組刪除並且不誤刪 圖片位置標記群組

  // 目前實作清理控制符程序都會有一些問題,在觀察一下

// 2025/11/03 今日繼續逐步完成依據 byte 機制,進行防治的 文字處理函式的重做

  // 實作了一個判斷小函式

  // 發現實際需要做的事情是檢查目標周圍的字串以此還避免誤刪

  // 需要修改工具小函式,連同周圍的byte也要一起檢查才行

  // 測試看看 清理pard 函式是否可行 結果 : 失敗 依舊會導致亂碼

  // 觀察修改後的檔案: 如果是中文結構被破壞,因該只有中文區會亂碼,但是實際上是整個檔案都變成亂碼

  // 目前的亂碼應該是結構問題,而不是只與高位保護有關(但高位保護也還是需要觀察)

  // 目前先朝著 \pard 不能單獨刪除,要連同後方連著的一串控制碼一起刪除的思路去嘗試

  // 嘗試成功 : 確實不能只單獨刪除 \pard 控制符 要整組一起刪

  // 中文 byte 的範圍重疊可能性, ansi 編碼 : 第一個 byte 不會 , 第二個 byte 有機會 , utf8 每個 byte 都不會

  // 已經完成 \pard 控制段落的清理 ,接下來實作換行控制符的更換

  // 根據效能等問題考慮,清理大量控制符與控制符因該使用 重構交換法 

  // 開始實作 換行符置換函式 , 失敗 : 沒有換掉,還破壞了整體結構

// 2025/11/04 觀察函式失敗原因 , 懷疑是因為高位保護機制沒過導致失敗

  //因為此種換行符都緊貼正文結尾,因此因該只要檢查尾巴後方有無 高位元(中文byte)即可

  //使用 預設參數 的方法其可以決定要使用 哪種版本的高位檢查

  //方向對了,但需要調整一下

  //檢查需要更嚴謹遇到換行符直接過有導致誤判的情況發生

  //目前的亂碼很可能來自前方的圖片標記群組 我因該先把群組刪掉只留內部的純文字標記

  //因該改為在一開始清理控制群組時就一起執行

  //懷疑與記事本有關
 
  //有額外寫 BOM 的會正常

  //使用 std::wofstream 的代價非常高,也許我因該先理清是否值得這麼做

// 今日要先釐清 使用 std::wofstream 帶來的限制,以及決定是否需要使用

  //需要一步步確認並測試使用 寬字元版本的必要性

  //第一步 使用 wmain() 有必要性,這樣才有辦法讀取到中文檔名,並取得需要的命名名稱

  //開啟輸入檔案.用一個字串讀取輸入檔案的內容:沒有問題

  //偵測:只有讀取,沒有修改 : 沒有問題

  //建立資料夾 : 考慮到需要中文名稱,依舊使用 wstring :沒有問題

  //關鍵點 : 建立輸出檔案時能否使用 std::ofstream ,而不是使用 std::wofstream

  //修改成　ｓｔｄ：：ｏｆｓｔｒｅａｍ　，測試看看有無問題

　//改用 std::ofstream 沒有問題可正常開啟檔案,且名稱也正常
  
  //已經將輸出檔案修改回 std::ofstream 並使得顯示正常,因此可以繼續實作文字修改函式

  //修改架構後,文字修飾都已經正常可用

  //實作文字清理中的最後清理

  //需要稍微修改之前函式的邏輯

  //已經成功清除掉最後的控制符們,現在只需要再進行小修改即可

  //沿用原本的空行數量限制 以經成功

  //實作最後一步,清除外層的大括號,並且不要動到 內文中的大括號,並保證 {} 處於平衡狀態

  // 函式目前問題可在無法由後往前找到右大括號

2025/11/06 找到沒有定位到右大括號的原因

  // 詢問後發現可能原因出在, std::isspace 只會對 ancii 編碼內的空格與換行符有正確判斷,因此在utf 檔案內其可能無法正確判斷

  // 需要實作自己的 std::isspace

  // 實作完自己的函式依舊沒有動,可能需要先搞清楚尾八的元素到底放了啥

  // 結果: 是因為結尾多一個 '\0' 導致誤判跟 utf 體系無關

  // 已經成功移除最外圍的 左右大括號

  // 最後清除開頭多餘換行符,可以開始對更多檔案進行測試了

  // 開始分別對測試檔案進行測試

  // 含圖片檔 : 成功

  // 含特殊符號的三種編碼檔案

  // 顯示正常,但是utf 系列的排版有點奇怪,需要觀察一下 utf系列的其餘檔案

  // utf8 體系的段落被破壞了,先懷疑是群組處理的問題

  // 查證了一下,是因為比較高級的文件程式(word,Libreoffice) 會額外用資訊對空行的大小距離等東西進行加工,但是記事本就沒有這麼高級的功能了

  // 所有測試檔案都成功可以繼續階段目標了

  // 新的階段目標 : 錯誤處理：遇到無法解析的區塊，能 跳過 + log 記錄，而不是整個程式掛掉。

  // 分清階段 : 錯誤訊息因該根據程度決定:1.立刻結束並告訴使用者 2.可繼續執行:跳過錯誤段落,並將錯誤段落放入 log 中

  // 檔案 I/O階段 : 狀況 1 此階段不屬於可跳過,之後實作 GUI 在完成

// 2025/11/07 先擬定好高如何實現 錯誤跳過 +  log 記錄 的方法

  // 策略:在前期先採取, 紀錄錯誤段落 + 忽視該段落繼續執行 的方向

  // 先創立一個類別專門負責此事

  // 先釐清該類別需要什麼資料成員

// 2025/11/10 先釐清如何實作此系統

  // 錯誤系統處理的分類 : 直接中斷執行 與 可繼續執行但是要跳過錯誤部份 + 紀錄

  // 思考是否將這兩種錯誤處理都實作為類別(看是繼承同一個介面,還是裝在一起)

  // 藍圖 : 先實作 IErrorHandler 介面 , 都繼承後分別實作 中斷並直接回報 與 繼續執行並記錄錯誤資訊 兩種類別

  // 依照檔案流程來實作各自的錯誤類別

// 2025/11/11 今日先嘗試實作中斷版的錯誤類別

  // 在介面中先加入兩種規則, 顯示錯誤 與 顯示錯誤後強制停止程式

  // 思考介面規則所需的分類 

  // 目前有三種情況 : 1.只回報錯誤訊息 2.回報錯誤訊息並中止整個程式 3. 單一檔案錯誤只跳過執行該檔案

  // 目標是在函式中 : 所以我只需要用 只回報訊息的版本 + 直接return 就可以實現目標 , 不用再加入新的

  // 新增函式專門去建立資料夾並做安全性檢查

  // 已經依照目前流程順序將系統性的錯誤處理包裝進專門的類別中

  // 接下來實作 繼續執行 + 紀錄錯誤段落的類別

  // 嘗試用暫時物件分享路徑已被證實不可行

  // 將目光放到專門儲存所有需要資訊的類別上

  // 在新增功能的路上需要增加越來越多的工具,需要將資料傳來傳去很混亂,想要將資訊都放入專門的類別,其餘資料直接拿該類別的指標去取得資訊就
     好

  // 思考 安全性與回傳值

  // 測試過程中發現之前做的資料夾建立函式有問題

  // 改好了

  // 目前改用 fileContext 將所需資料放入同一個類別中 已經 在圖片函式成功使用,接下來看要能不能延伸使用在別的函式(類別) 

// 2025/11/12 今日嘗試將資料庫概念繼續延伸給其他類別

  // 確認偵測類別先於 FileContext() , 並且會將結果傳遞給它

  // 依照流程先處理偵測區塊的錯誤系統處理

  // 偵測階段只有 : 目標並非 rtf 檔案才視為必須終止的致命錯誤

  // 在偵測類別的開頭先加入 偵測是否為 rtf 檔案的獨立流程

  // 建立了非 rtf檔案 中止執行的流程

  // 簡易測試有成功運作

  // 沿著流程嘗試將實作圖片處理區的錯誤系統

  // 先架設好基本的處理類別架構,等到實際使用再去做調整

  // 建立了 私有的檔案建立函式,等到真的發生錯誤再去建立檔案即可

  // 已經架構基本結構,接下來進入圖片區塊

  // 先釐清到底要記錄什麼

  // 詢問後的建議 : 最佳實務：只記錄摘要 + 片段

  // 以圖片區塊來講 如果有錯誤因該要將該區塊整個替換成標記符.不能留下一串十六進制的圖片區塊

// 2025//11/13 先思考如何判斷圖片是否正確處理,以及判斷完成後該如何處理

  // 先討論演算法 : 什麼情況判定要進入到 錯誤系統處裡的區塊 

  // 將圖片處理階段分類

  // 錯誤系統因該放在 第二段 : 等到確認完位置,擷取該位置去進行執行判斷

  // 所以我需要一套錯誤判斷類別,各自實作自己的錯誤判斷

  // 目前藍圖 : 建立一個全域的錯誤類別 enum -> 建立一個各自的錯誤判斷類別 -> 使用一個統一的錯誤處理系統

  // 建議是建立複數的全域狀態 enum 並且使用一個類別包裹由此可以有更多資訊可以決定如何處理

  // 已經建立了複數錯誤相關資訊的enum , 並將其放入了一個類別包在一起使用

  // 使用介面將每種錯誤偵測連結在一起,這樣最後的 errorHandle 就不需要知道知道來源是啥

  // 開始實作圖片區塊的錯誤偵測類別

  // 釐清是否要直接使用定位圖片區塊時的資訊 , 結論 : 不要,將定位與檢查分的清楚

  // 得到的建議 : 初期可以先將各自的任務寫成各自的函式各自執行 , 等之後達到效能瓶頸再考慮合併

// 2025/11/14 繼續完成錯誤偵測類別的實作

  // 偵測錯誤時因該要能至少完整掃描一次,並能將找到的錯誤一次回傳
  
　// 修改了存處錯誤偵測資訊的類別使其能夠存放復數狀態的錯誤資訊

  // 已經完善類別架構接下來只需要依序實作需要檢查的東西

  // 需要先完成的必做 四個錯誤檢查 : 1.結構錯誤 2.控制碼錯誤 3. 內容資料錯誤 4.邏輯錯誤

  // 以實作 結構完整度檢查 (大括號是否平衡)

  // 嘗試實作 控制碼錯誤檢查

  // 因為目前只有提供 轉換為 WMF 圖檔的檢查 , 因此只先檢查是否為 wmf 型態

  // 因應目前的功能 : 對於控制符的依賴性還未產生(等到支援更多圖片格式才有需要) ,因此暫時不需要控制符錯誤偵測

  // 改為 優先實作內容錯誤檢查

  // 先想辦法正確的判斷是正文開始 , 可以去文字清理區找找
 
  // 需要從 rtf 文法的電腦角度去找到控制符文法的中止點,用人眼看不準

  // 被搞了= = 需要找看看正確停止的方法

  // 跟gpt 繞了很久的圈子, 此函式正常程式可用,因為實務上都會用 空格 或換行 隔開

// 2025//11/17 藉由上次的函式取得內容區塊後對內容區塊進行正式的檢查

  // 實作內容檢查

  // 一般內容錯誤偵測 : 是否都是 HEX 碼 , HEX 碼是否為偶數(其特性)

  // 檢查檔案是否符合 WMF 檔案的格式

  // 實作檢差 wmf 頭部格式需要的轉換工具

  // 已經初步完成圖片錯誤偵測類別,如果大致可行將嘗試進行融入原有程式中使用

  // 小修了一下將嘗試將其融入原有程式

  // 初步融入錯誤函式,現在需要實作負責全部錯誤處裡的 ErrorHandle

  // 當初將整個錯誤分成兩個類別處理其實不好處理,因該統合成一個好讓所有錯誤偵測的結果都丟給同一個處理

  // 整合錯誤處理系統,好讓偵測結果可以流暢處理

  // 也許之前的錯誤 也該寫成 ErrorInfo 統一處理方式

  // 將錯誤處理管線全部改成 ErrorInfo -> ErrorHandle 的模式

  // 將在 主程式 main 創造 ErrorInfo 變數 並使用參考進行傳遞 

// 2025/11/18 完成錯誤線路統一成 ErrorInfo -> ErrorHandle 的模式

  // 探討使用何種架構

  // 如果很確定是ｆａｔａｌ　等級的錯誤（I/O），因直接呼叫專屬函式直接中止

  // fatal 錯誤處理也分成 : 全域型(中止整個程式) . 區域型(複數檔案中某個檔案壞掉,跳過該檔案繼續執行並記錄下 errorlog)

  // 實作區域型 fatal 處理

  // 區域型的就需要紀錄 errorlog 了  可以趁此機會完成 ErrorInfo -> ErrorHandle 的實作後續也可以重複利用

  // 在檔案進入後就因該直接建立 輸出資料夾了

  // 為了區分 是單獨的檔案還是資料夾因該額外傳入一個 enum 來進行分辨

  // 新增 enum 區分是 處理單獨檔案 還是 一個 資料夾

  // 已經依照 enum 來決定使用 哪種 fatal 處理

  // 需要實作 紀錄功能了

  // 需要先解決 errorlog 該放在何處的問題(也就是存放路徑處理)

  // 資料夾模式在迴圈前先確保建立了輸出資料夾

  // 已經給予路徑,並且設定了在需要寫入錯誤時加入 errorlog.txt 的設計

// 2025/11/19 已經確認了目標資料夾以及輸出文檔,接下來將實際寫入錯誤訊息

  // 嘗試實作將錯誤資訊寫入 errorlog 的函式

  // 遇到問題了,沒有運算子(<< ) 支援 enum 寫入到 std::ofstream 裡面可能需要自己先轉換或自己實作 

  // 目前最低需求 : 至少每一個自己創造的 enum class 都需要實作 各自的 operator<< , 之後最好在包裝起來方便使用

  // 先搞定各自實作的部分

  // 因該先用同一個命名空間將 各自都幫起來 (名稱一樣在編譯器中會視為同一個命名空間) 

  // 實作了其中一個看看可不可行

  // 看起來還 OK 依樣畫葫路繼續實作另外幾個

  // 有推薦使用 enum 特性來改用 陣列 儲存名子的方法,可以考慮使用 

  // 因為 enum 是固定大小(不會變動),因此容器選擇 std::array 是最優選

  // 嘗試實作使用 std::array 的版本

  // 實作完成另外兩個類別用同樣方式實作

  // 已經使用 std::array 的方式完成了 三個 enum 各自的 operator<< 函式實作 接下來要將其整合到 整合型類別中使用

  // 將全部進行整合 , 最後使　ＥｒｒｏｒＨａｎｄｌｅ（）函式可以很直覺的　＜＜ＥｒｒｏｒＩｎｆｏ

　// 將做完的工具繼續整合進原有的流程之中

  // 已經將輸出資料夾部份處裡一個段落了,可繼續往下走

// 2025/11/24 可以依據上次實作的錯誤系統開始正式套用

  // 無法開啟檔案的情況 : (已經實作)

  // 往下先實作了 : 偵測到非 rtf 檔的錯誤處理

  // 開始對階段 : 偵測 , 導入錯誤處理系統

  // 探討該如何正確的將 偵測流程 與 錯誤系統結合

  // 目前的偵測階段就是依據條件回傳 enum , 如果我今天想整合錯誤系統,就要新增一個enum 會比較乾淨 , 但是這樣原本的回傳值會整個亂掉

  // 找尋統合的方法 或 評估是否值得全部重新設定

  // 問到一個覺得很有嘗試價值的方法,將回傳值改為新創的類別,並讓該類別回傳複數所需要的資料

  // 先創造新的 enum 將可能的偵測錯誤放入

  // 嘗試將原本的回傳 enuｍ　改為回傳一個物件

　// 先新增預設值來避免未設定就進行使用

  // 修改時發現原本的偵測類別過於壅擠,因該把對 rtf 格式的檢查從各個 編碼偵測的類別中抽取出來避免責任過於混淆

  // 目前已經有 : 是否為 rtf 格式的簡查(查開頭)

  // 嘗試新增 : 檢查檔案是否過小(是否小到連 rtf 格式都放不下)

  // 新增 rtｆ標頭　格式檢查

　// 已經將 rtf 前置檢查抽離出來

  // 繼續 各自編碼偵測的調整

// 2025/11/25  完成回傳值的改造

  // 類別都已經調整,剩下有用到的需要調整

// 2025/11/26 以建構初步架構 , 探討是否可行

  // 需增強 ansi 編碼類別的判斷

  // 因應流程增加了避免誤判: utf 體系為 ansi 體系的機制

  // 發現該判斷很容易誤判,需進行更進一步的判斷

  // 發現因該 判定\uN 控制符 不是判定 \u 

  // 需要思考現階段偵測是否就需要加入 如果遇到錯誤 ansicpg 的處理

  // 所需工程非常龐大,因該放到第六階段才對

  // 處理完偵測結果就開始將結果拿去處理

  // 感覺目前的處理已經纏在一起了,因該找一個方式分的清楚一點 

  // 改成傳入函式中處理,在依據函式的回傳值 決定主程式是否該繼續執行

  // 將函式大致上架構完成 ,開始填入資料

// 2025/11/27 先把所有資料填完

// 2025/11/28 已經暫時完成偵測部份的錯誤系統整合,繼續往下

  // 開始進行圖檔階段的錯誤系統設置

  // 將之前做好的圖片錯誤偵測結果 與 現有的 錯誤處理 結合, 直接完善圖片處理區塊, 可繼續往下

  // 依照一樣的方式, 開始建立解碼類別需要的錯誤偵測

  // 思考如何實作,因為這三種編碼 需求並不相同

  // 先實作 utf8 跟 ansi 各自的 utf16 的 等處理轉譯動作在處理

// 2025/12/01 探討是否能用延續的結構來完成各自的類別

  // 想法:先實作一個共通的解碼的錯誤偵測(兩種編碼都會用到),在實作兩個新類別去繼承,新的類別在實作各自編碼要偵測的錯誤

  // 太耦合無法繼承在繼承 , 因該先實作一個共用的大家都走 , 在依據各自的編碼偵測各自的錯誤可能

  // 也就是在進行解碼前因該先檢查一輪,圖檔處理完的文字檢查其是否符合 rtf 的文法

  // 實作 rtf 文法檢查

  // 實作括號平衡檢查

  // 實作 控制符文法檢查  

// 2025/12/02 完成 rtf文法檢查類別

  // 完成 ASCII字元 是否為合法範圍的檢查類別

  // 完成 是否為空 byte 檢查函式

  // 完成 圖檔是否有剩餘資訊檢查

  // 傳回主程式,思考如何處裡

  // 決定新增一個 統一處理 errorInfo 的類別

  // 不因該混用 enum 因該用一個新的

  // 新增一個配套的 enum 以後處裡 info 就可以使用

  // 已經晚成共通的 rtf 文法層檢查,接下來要依據各自的編碼實作 各自的錯誤偵測類別

  // 實作了一份 utf8 體系的 錯誤偵測類別

  // 思考如何統合進原本的主程式

// 2025/12/03 嘗試在主程式中處理傳回來的錯誤

  // 已經做了基本架構,之後的只要進行串聯即可

  // 嘗試實作 ansi 版本

  // 與 utf8 有著一致的標準不同 , ansi 會隨著其不同的 ansicpg 有著不同的標準需要先搞定這一塊

  // 新增資料成員存取偵測到的 ANSI 狀態
 
  // 新增防呆

  // 想法 : 新增各種 codepage 的判斷到 AnsiErrorDetector() 的私有成員函式 , 之後依據資料成員 emc_ 去決定呼叫哪個

  // 已經完成全部的架構,接下來依序填完各自的 codepage 條件就好

  // 先做 Big5 版本的

// 2025/12/05 完成所有函式

  // 完成 Big5 版本

  // 完成 GBK 版本

  // 完成 JIS 版本

  // 完成 Korean 版本

  // 完成 Latin_1 版本

  // 完成 CEI 版本

  // 完成 Cyrillic 版本

  // 初步完成 ansi 的偵錯類別,嘗試結合進主處理程式中

  // 初步結合成功

  // 接下來嘗試對 utf16 轉碼階段加入錯誤處理功能

  // 因為處裡方式是將 utf16 轉成 utf8 , 因此在 utf16 轉譯階段就該將其錯誤找出並處理

  // 需重做 utf16 轉碼類別

  // 不該將兩個合在一起,因該實作偵錯,等OK 就直接轉碼不需要再排查 

  // 先實作 utf16 類別的錯誤偵測

  // 完成了 utf16 錯誤偵測類別,之後嘗試放進執行程式中

// 2025/12/09 整合程序

  // 已經架構好 

  // 將之前混合功能的轉譯器,改為純粹的轉譯功能

  // 已完成,實作最後部份的錯誤處理功能

  // 思考 文字清理部份的錯誤偵測需求

  // 最終文字清理不因該執行錯誤偵測,因為其做用只是做 資料美化而已

  // 開始對錯誤系統進行實際測試與調整

  // 擬定測試流程

  // 第一步 : 進行基準測試 : 找一份之前正常的檔案,跑跑看會出現啥問題

  // 問題 : 有順利的建立出 errorlog 檔案 , 並寫入錯誤資料(沒亂跑) 但是沒有順利跑完

  // 另一個檔案也是停在同一個錯誤,可以初步判斷該錯誤偵測設定有誤或過於敏感

  // 依據錯誤系統資料研判,是格式錯誤判斷部份的問題

  // 要處理兩個問題 : 1.為何會觸發?(不符合預設的觸發條件) 2. 其寫入的錯誤型態有誤

  // 以解決錯誤寫入資料型態錯誤之問題

  // 目前:文法檢查方面容易非預期觸發, 以及檢查 \rtfN 那個數字也容易觸發,其餘都沒被觸發

  // 已經修正 檢查 \rtfN 那個數字N容易觸發 的問題

  // 嘗試修正文法容易觸發的問題

// 2025/12/10 繼續解決[基準測試]在文法檢測區的意外觸發問題

  // 發現連續錯誤寫入太密集,先進行調整

  // 修改建立輸出檔案的時間點

  // 調整連續錯誤寫入 讓其不會連在一起

  // 嘗試找出文法區錯誤觸發原因

  // 移除 nullbyte 檢測,解碼前作此檢測必定會觸發

  // 嘗試找到錯誤觸發的控制字為何

  // 發現 RTF 文法規定超多,不可能全部遵守,目標因該是檢查會用到的控制符能夠正常運作

  // 修改了原有的RTF 文法判斷

  // 再次進行[基準測試]

  // 修改後 [基準測試] 依舊無法成功

  // 找到觸發的重要嫌疑人 : printf("H   H\\nH   H\\nHHHHH\\nH   H\\nH   H")

  // 解決其中一個觸發點 : 標記註解用的雙反斜線 \\ 導致判斷錯誤

  // 兩個錯誤觸發都已經處理 , 第一次通過 [基準測試] , 開始對更多檔案進行測試

// 2025/12/11 對更多檔案進行[基準測試] 以觀察是否有更多情況

  // 對有 一張圖片檔的檔案進行測試

  // 圖片區有問題需要調整

  // 是 wmf 格式檢查函式錯誤觸發

  // 找到一個新的試試看

  // 至少可以確認 錯誤圖片不影響翻譯流程,並且會掛上正確的標記符

  // 去原檔案找問題看看

  // 嘗試修改可能的觸發條件

  // 目前可以確認主要問題就是 wmf 目標認證函式的問題,因該嘗試先找出其觸發錯誤的條件

  // 將條件拿去分析 , 發現實務上過於容易被觸發,作用也不大因此直接拿掉

  // 已經成功完成 [基準測試] , 在對更多檔案做測試

// 2025/12/12 對更多檔案做測試

  // 將目標換為 整個資料夾試試看是否能行

  // 其中一個 目標資料夾有問題 : 建立的 errorlog 不是在預定的原資料夾中

  // 以解決目標建立資料夾地點錯誤的問題

  // 有兩個重要的東西要解決 : 1. 目前的 errorlog 缺乏必要資訊 : 是哪個檔案 2. 就算判斷是終止型錯誤也會建立資料夾

  // 需要重作:資料夾建立系統避免混亂

  // 先從完善 errorlog 開始 

  // 需要先加入 : filr Header 的模式 標註是哪個檔案的問題以及其位置

  // 找到了適合的 begibfile 設定點

  // 嘗試加入流程到 errorhandle 中

// 2025/12/15 先把 errorlog 完善

  // 已經在原有的 errorlog 中成功加入了 filerHeader 

  // 加入結尾的概念使 errorlog 更為完整

  // 新增結尾資訊所需之資料成員

  // 已完成結尾所需資訊計算系統

  // 先確立結尾整體流程

  // 先確定好對應的關閉程式 endFile() 需要關掉什麼

  // 初步完成 endFile() 接下來看看如何放到主要運行程式中

  // 被推薦 Scope Guard 看起來很棒

  // 嘗試加入

  // 試試成效

  // 已經成功加入想要的資訊,可開始處理 : 就算判斷是終止型錯誤也會建立資料夾
 
  // 先觀察一下程式處理 建立資料夾的部份

  // 調整建立資料夾的邏輯 : 如果有資料產出才建立輸出資料夾

  // 要修改建立資料夾的時機 : 改在在確定有產生輸出資料前,才生產出資料夾

  // 需要先去抓出所有會產出資料前的時機點

  // 重擬了一下 :　因該是先把創立資料夾的概念包裝成一個新的類別專門負責，之後再將其融入原程式中（不再直接建立）

// 2025/12/16 先將建立資料夾包成一個類別

  // 整合有點問題

  // 想要修改遇到的問題 :　資訊的傳遞會有問題，改成延後建立的話無法明確告知資料夾的位置需要修改舊有的機制

　// 目標是改為傳遞可以建立資料夾的類別而不是只傳遞位置

  // 需要先把新建立的類別功能釐清才能順利整合進原本的流程

// 2025/12/17 目前卡在建立資料夾

  // 不因該把路徑判斷再放給工具,因該在策略部份就確定好路徑

  // 因為 errorlog 有不同的需求因該將其跟一般輸出(txt檔,Pict檔) 分開

  // 因為有分流因此會需要各自的建立資料夾負責類別

  // 發現只需要原本就有的工具就可以了不需要再做一個新的,只需要在使用上給予不同的名稱即可

  // 我現在需要的是負責不同任務的'物件',而不是不同的'類別'

  // 已經確定有兩個可以用的 建立資料夾物件,並且應對不同的目標型態會有不同流程,接下來將嘗試整合這兩個物件進入原有流程中 

  // 先從 error 的建立資料夾物件開始將其交給ｅｒｒｏｒｈａｎｄｌｅｒ

　// 原本的參考會有衝突這裡必須使用指標

  // 已經初步整合 error 專用的建立資料夾類別放入 errorhandler 中做使用了,接下來換成一般的

// 2025/12/22 嘗試將一般資料流的專用類別整合進流程

  // 修改以往的步驟 : 因為不再預先建立資料夾所以不再透過 fileContext 傳遞 資料夾訊息 而是直接將該'能力' 傳遞

  // 需要修改 圖片類別 使其可以使用 預定類別

  // 已經初步將 圖檔 區 改成需要再建立輸出資料夾的模式,不在依賴預設資料夾路徑

  // 也在寫入輸出文檔前加入此步驟,至此算是初步完成改造將對原本的測試資料夾進行實測

  // 目前正常檔案沒有被影響可順利執行,但是建立資料夾會卡住需要調整

  // 先找出建立資料夾是卡在哪裡

  // 已經確認是卡在 建立資料夾的這一行

  // 推測是誤判導致,重新設定建立資料夾的行為

  // 目前看來是在設定 針對資料夾的檔案時路徑給定錯誤

  // 已經確認問題是目標為資料夾時 給定的路徑問題

  // 需要重新設定原函式 目標為資料夾時需要額外傳入原目標資料夾的路徑

  // 使用 std::optional<path> 當第四參數,並預設其為空 目標為單一檔案時不需要特別理會就可以使用

  // 已經修復問題可正常使用

  // 兩個目標都已達成 : 1.errorlog 已經可以明確告知是哪個檔案的問題 並且有更詳細的解釋 2.資料夾的建立已經改為確認有輸出之後才會建立

// 2025/12/23 排除錯誤觸發原因(原檔案為正常)

  // 問題出在原函式的依 byte 檢查的設定上,不適合放在一開始的檢查,因該放在全部轉為utf8 之後再做檢查
  
  // 決定先移除該檢查 :　作用很小，並且常常誤判

　// 審視文法檢查 其常常在其他基礎編碼上衣外報錯

  // 已經調整順序解決了問題,可以開始對其他檔案進行測試

  // 回頭測試改動是否有影響 單一檔案的目標

  // 單一檔案正常對之前的所有檔案做測試

  // 之前的所有檔案都已經通過基準測試,詢問看看還需要什麼樣的測試

  // 目標 : 找一個正常的檔案並加入錯誤條件 , 測試錯誤系統能否真的抓到

  // 已經初步完成 錯誤系統, 開始新的目標 : 更多輸出格式

// 2025/12/24 擬定增加更多輸出格式目標的計畫

  // 先從三種推薦的新格式開始 : 1.Markdown(md) 2.HTML(html) 3.JSON()

  // 需要先增一個中間層在檔案寫入前依據需求,新增語意進入

  // 不是加工字串,而是去解析該字串

  // 從抽象層面來說我的任務 : 
      
      //1. 讀取「已解碼、已通過語法檢查的 UTF-8 字串」

      //2. 將其「解析為不可變的語意結構（Document Model）」

      //3. 之後所有輸出格式，只讀這個結構，不再碰字串

  // 目前的第一階段 : 解析 std::string 建立 語意結構

  // 詢問如何實作

  // 第一步:架設好我的 語意結構框架

  // 可以先找一個簡單的 之前產生的 .txt 檔以此來回推出 初始架構

  // 沒有頭緒需要範例

  // 依據範例嘗試抓看看

  // 嘗試抓了 但 要如何運用?

  // 詢問後發現是為剛剛所有的工具(Paragraph, textNode,imageNode) 分別實作專門的類別

  // 先完成 兩種節點的實作(Textnode imageNode)

  // 已經架構了基本所需的東西,接下來可以依據實際使用情況調整

// 2025/12/29 嘗試實際建構語意結構

  // 先在另一個測試檔案中 嘗試使用,試試看此工具

  // 5個工具的理解 : node 這三個是一組的 其代表的就是各自一段的訊息,並依照其屬性(文字,圖片) 進行初步區分,是組成整體資訊的基礎

  // Paragraph 段落 本質上是一段陣列,其中的每一個元素依靠 std::uniaue_ptr 的特性,可去確保其唯一性(不可複製),以及確保其會被正確移除

  // Document 整體 : 用來存放所有段落

  // 已經在測試檔案中初步建立了物件並做了初步的使用,接下來嘗試下一步 解讀建立物件中的資訊

  // 先寫一個 txt 版本的 Render 做測試用 

  // 測試成功 已經可以正確解讀測試輸入的文字

// 2025/12/30 進一步的對測試檔擴充

  // 先修改之前的 render 函式 , 將此功能加入節點的介面中,便可以不再需要使用 判斷式

  // 已經成功將顯示功能放到節點類別中此後就可直接呼叫

  // 下一步 建立 DocumentBuilder 類別 確保能夠將之前的字串正確的變成語意結構

  // 建立迴圈開始考慮各種條件

  // 第一種條件 : 遇到圖片標記符

  // 使用兩段式判斷 : 第一段 : 快速判斷(方便挑選,減少複雜判斷機率) 第二段:仔細判斷(避免混入不符合規定之標記符)

  // 嘗試依照問來的演算法實作出程式

  // 完成第一段,巡查問題

  // 嘗試修改第一段

  // 暫時完成第一段等第二段也完成後就進行測試

  // 嘗試實作第二段:條件2 遇到空行判定為段落

  // 巡查問題

  // 巡查整個函式

  // 已經做了一個類別出來,實測看看其效果

  // 使用測試用字串來進行實測

  // 測試結果符合預期

// 2025/12/31 在進行一些小測試就能將其整合進原本的流程之中

  // 測試1 : 圖片前後沒有空行 (正常)

  // 測試2 : 連續圖片(正常)

  // 測試3 : 非法圖片標記(目前預設是當作一般文字) (符合預期結果)

  // 測試4 : 連續空行(正常)

  // 已經可以算是初步完成架構,可以將其整合進原程式中

  // 已將測試檔案所創之工具轉移回主程式

  // 在保留舊有的流程的情況下 嘗試加入新流程產生結果以此來對比兩邊輸出的差距

  // 已經修改成雙輸出模式,可開始測試效果

  // 對單一檔案測試的結果為 : 正確

  // 對資料夾型檔案實測

  // 資料夾型態實測結果 : 正常

// 2026/01/05 經過實測可以將輸出路線改為 單獨使用 語意模型這條線路

 // 進入下一個階段也就是最初的目標:擁有更多輸出模式

 // 依據之前的架構 我只需要開始建構 其他模式的專屬 Render 就可以產生一個初步的結果

 // 開始嘗試建立 md 專用的 render類別 

 // 已經建立 md 專用類別,嘗試建立測試檔

 // 使用雙輸出 看看 txt檔 與 md 檔的輸出會有何不同

 // 編譯錯誤 尋找問題

 // 不小心 複製 導致違反 std::unique_ptr 不可複製的規定 已修正

 // 對單個檔案進行測試

 // 比對別果為 : 正確

 // 對 含有圖檔的 資料夾 進行測試 

 // md 模式可以算是初步可用,接下來先在擴充 html 模式

 // 建立專用類別,並新增 node 對應的處理函式

 // 已經初步建立,接下來正確建立檔案後就可以開始實測

 // 嘗試正確建立 html 文件

 // 先對單一檔案進行實測

 // 測試結果:成功

 // 嘗試對含有圖片的 資料夾 進行測試

 // 發現一些問題 : 1.排版消失 2.圖片標記符遺失

// 2026/01/06 嘗試修正目前兩個 HTML 發現的問題

 // 圖片問題可能只是不支援導致看不到

 // 在 html 規定裡面依靠 <p>...</p> 來區分段落 

 // 得到的建議 改由 node 的功能 自行決定開頭的段落或結尾

 // 嘗試用建議的安全方法重寫函式

 // 單獨使用換行符 \n 做為 html <p> 的依據 會導致文件格式的混亂需要更有邏輯的依據

 // 真正因該整的是 DocumentBuilder 這個類別工具 在將字串拆解成語意結構時就因該紀錄好排版的狀況

// 2026/01/07 先重整目前的 DocumentBuilder 條件 , 在嘗試修改

  // 條件1 : 遇到預設的圖片標記符就新增 imageNode 節點

  // 條件2 : 遇到連續換行(\n\n) 的情況下判定為段落結束,將目前儲存的字串存入 TextNode 節點,並更新節點與字串

  // 非上述兩個條件就將掃描到的 字元 逐個放入 暫存的字串中

  // 結尾  : 檢查 暫定字串是否為空 非空的話就當作最後一個節點加入 

  // 先修改條件1 的運作方式 : 不再是只新增 node 額是直接更換新的 Paragraph

  // 發現如果繼續在 Paragraph 裡面放兩種節點,後面會越來越混亂,需要創造一個新的專門放 imageNode 的段落才好處理

  // 嘗試使用繼承 將放置節點的容器做區分

  // 創立存放段落共享的介面

  // 初擬類別

  // 修改使用類別

  // 嘗試將新的類別融入原有的流程

  // 需要修改原本的段落容器

  // 依據新的設計建立了新的容器,嘗試在 DocumentBuilder 中改成使用新容器

  // 發現需要先理解並實際運用 indent 的數字功能

  // 為了實現排版的功能需要改成一行行的讀取

  // 開始填充每一行讀取的條件

  // 可以把每個條件做成私有函式方便進行判斷

  // 私有函式工具 : 清除左方空格已完成

  // 條件之一 : 遇到空行的反應已經設定

  // 開始設計私有函式工具 : 判斷是否遇到預設的圖片標記符
 
// 2026/01/08 完成私有函式工具

  // 已經完成判斷工具函式

  // 嘗試完成判斷是圖片標記符後進行,空間新增的動作

  // 已經完成新版的 DocumentBuilder 

  // 需要修改之前的 render 

  // 以後的 render 只依據其拿到何種 block 去決定進行何種處理

  // 完成了 textrender 但要先搞清楚

  // 已經先依樣畫葫蘆地完成了 txt 跟 md 兩者的 renderer 測試看看效果

  // 編譯失敗 遇到看不懂的錯誤訊息

  // 跟 std::unique_ptr 有關

  // 詢問後跟 無法建構 TextNode 有關

  // 已排除問題(介面修改沒有跟繼承類別連動) 開始測試

  // 修改細節

  // txt 與 md 暫時可用了 換 html

  // 嘗試實作有正確段落及排版的 html render

  // 已完成 html render 進行實測

  // 對單個檔案實測成功 對資料夾進行實測
 
  // 測試成功 已經初步新增 html 的模式

  // 嘗試新增最後一個模式 .docx

  // docx　主要要改的點是需要導入第三方工具（不能用　ｓｔｄ：：ｏｓｔｒｅａｍ＆）　直接輸出　因該先走　圖片轉換

// 2026/01/09 新目標 : 新增圖片輸出格式 .png

  // 因該先釐清原有的圖片分離設計的問題

  // 目標不因該是 : 新增 wmf 轉 png 功能,這只是可選的加值功能

  // 目標 : 能夠正確處裡 rtf中 常見的 圖片格式 

  // 需要先對 Disassemblypictures 進行調整,這是負責圖片處理的主要函式

  // 先從演算法的層面來了解依據新需求需要做到的事情

  // 不用直接重寫整個 圖片處理函式

  // 先新增一個函式 可以將找到的圖片區塊中的 圖片控制符正確解析

  // 需要重新架構 圖片處理函式,舊有的圖片處理 假定是 wmf 格式的處理將過多任務綁定在單一函式上面不利於擴充

  // 需要架構新類別將任務分散以利於後續的擴充

  // 將確保有輸出資料夾的功能綁訂到 新類別上

  // 先建立 管理所有圖片流程的 圖片總控類別

  // 對 Disassemblypictures 已經確認的功能搬進 process 做低風險重組 

  // 先嘗試是否能正確重現原功能

  // 進行實測

  // 已經確認具備原有的功能

  // 開始嘗試將進行程序私有化

// 2026/01/12 繼續完善圖片流程改進

  // 將 尋找括號結尾的while 迴圈改造成私有函式

  // 試試看 私有函式化後是否有影響功能

  // 第一個私有化函式 : 搜尋群組尾部 : 成功

  // 嘗試在類別中加入找出關鍵控制符的功能

  // 需要先設定可以吃到開頭控制符的條件

  // 決定先完成實現一部份功能的函式 找到 控制群組尾部位置

  // 完成工具函式 已經可以初步找到  hex 區塊開頭

  // 已經取得 控制符的段落接下來嘗試實作判斷函式

  // 先實作擷取 格式的函式

  // 已經完成擷取 格式 的函式

  // 建立一個存放所有擷取資訊的類別

  // 已經先初步取得所需的控制符資訊,可以開始嘗試使用

// 2026/01/13 在取得標頭資訊後開始進入下一步

  // 開始處理 hex 區塊

  // 先依照之前的結果取得 hex 區塊

  // 透過函式取得 std::vector<uin8_t> 的 陣列

  // 將 舊有的 class imageProcessor 改為只負責 將 byte 轉化為對應的圖檔

  // 先把計數功能搬回總控

  // 已經初步完成類別工具的改造,測試其是否能實現原有功能

  // 錯誤:沒有判斷出是 .wmf 的圖片

  // 先看看是不是 pictheader 判斷失敗

  // pictheader 沒有成功判斷格式

  // 已經確定問題是 位置抓取出現錯誤

  // 問題是呼出在開頭位置上

  // 已經成功將問題修正,並且成功重現原有的重現 .wmf 的功能

  // 嘗試使用推薦的功能 省去 substr() 的額外花費

  // 成功改用 std::string::view 省去了複製的功能

  // 可開始下一階段 : 新增 wmf/EMF 轉換為 png 的功能

// 2026/01/14 新增 將 wmf/emf 轉換為 png 的功能

  // 先測試 png / jpeg / emf 三種格式的檔案是否也可正確運作

  // 用 LibreOffice 製作 RTF 測試檔

  // 以確認圖片測試檔符合 測試需求開始測試

  // 在圖片偵錯階段觸發眾多問題 , 無法實際運作

  // 檢查了圖片偵錯函式, 發現其是因應早期的圖片製造需求而製作的因此先暫時關閉

  // 測試成功可正確處理 png 檔案

  // 測試 jpg 檔案

  // jpg 檔案測試成功

  // 測試 emf 檔案 :　跳過　現今檔案會轉成　ｐｎｇ　以後進行大樣本測試時在測試

  // 開始 新增 將 wmf/emf 轉換為 png 的功能

  // 測試看看 問來的 轉換方式是否可用

  // 轉換失敗 確定有啟用 轉換函式

  // 搜尋有問題的部份

  // 看起來是 呼叫 Libre 程式的路徑有問題,需要先去找出正確路徑

  // 已經修改成正確路徑,可確定不是路徑問題,繼續搜尋

  // 先驗證是不是因為檔案名稱含有中文導致錯誤

  // 已經確認 libre 程式有執行 但是 拒絕了 wmf -> png 的轉換

  // 改為採用 ImageMagick 進行任務

  // 需要先安裝 ImageMagick 才能執行

  // 已經 確認 ImageMagick 正確安裝並在 path 中執行

  // 已經 確認 呼叫 magic 可以將 wmf 檔案 正確 轉化成 .png 檔案

  // 函式轉化失敗,依據 log 找尋錯誤

// 2026/01/15 繼續修整轉化函式使其能夠正確運作

  // 已經成功使 純英文路徑 進行檔案轉換

  // 在 windows 的環境下為了支援中文檔名需要使用新的工具

  // 新增一個類別工具專門提供給 windows 環境使用

  // 測試新函式效果

  // 整合成功 已經可以對中文路徑正常作用

  // 對  圖檔資料夾進行實測

  // 已經確定獲得基本的轉換功能

  // 需重新設計圖片總控中間的 錯誤系統設置

  // 要拋棄舊有的 pictErrorDetector(會重複掃描),重新建立正確的錯誤處裡流程 

  // 新增了 圖片群組結構錯誤之處理流程

  // 新增了 搜尋圖片區塊控制符區域錯誤處裡流程

  // 已經成功重新整合了 圖片總控內的錯誤處裡系統

  // 測試成功,沒有影響原有功能

// 2026/01/16 新的目標整合 多執行緒的功能加入函式中

  // 先擬定好正確的演算法策略

  // 先架構好基本認知 :　需要多少執行緒

　// 因該用:預設自動 std::thread::hardware_concurrency() 由系統推薦數量 , 如果沒有就用 4 或 8

  // 統整的計畫表

  設計目標（最小可行版本 MVP）

  1.掃描資料夾所有 .rtf → std::vector<path> files
 
  2.決定執行緒數量：

    1.hw = hardware_concurrency()

    2.若 hw == 0 → hw = 4

    3.threads = min(hw, files.size())

  3.建立 thread pool（N threads）

  4.每個 worker thread 循環：

    1.從 queue 拿一個檔案

    2.processFile(path)（每個檔案獨立 output/log）

    3.處理完再拿下一個

  5.等所有 worker 結束（join）


  // 計劃的第一步 : 掃描資料夾有多少 rtf 檔案

  // 先觀察流程

  // 修改原有流程, errorhandler 完全可以到 處裡程序中在建立,這樣可以確保 thread 不會共享導致混亂

　// 測試修改修改後程式是否正常

  // 程式正常運行

// 2026/01/19 繼續加入多執行緒的任務

  // 先加入計時功能好明確知道 功能差異

  // 已加入計時功能,原有程序(跑 六個檔案的測試資料夾)所需時間 :　[Batch Finished] 耗時: 6907 ms (6.907 sec)

　// 繼續原有流程  先掃描資料夾內有多少 rtf 檔案

  // 建立一個類別工具, 用來進行 總管 多執行緒任務流程

  // 簡單實驗計數工具

  // 簡單測試通過

  // 決定執行緒數量

  // 簡單實驗看看 會有幾個多執行緒數量

  // 簡單測試通過

  // 建構正式流程

  // 實際測試程式碼是否可行

  // 轉換的 文字檔與圖檔正確,但是部分細節 std::wcout 穿插問題, 以及 errorhandle 是否會穿插疑問需要解決

  // 加入多執行緒後的跑檔案時間 :　[Batch Finished] 耗時: 2217 ms (2.217 sec)

// 2026/01/21 解決 thread 共用資源的保護問題

  // 先解決 std::wcout 被各種 thread 穿插的問題

  // 可以使用 mutex 來進行確保

  // 建立使用 std::lock_guard 來確保解鎖的 安全 std::wcout 以及 std::wcerr 函式

  // 將流程中會用到的部份 都修改成 安全型

  // 測試修改後的效果

  // 已經成功確保 std::wcout 的穿插問題

  // 確認目前的模式 一般輸出檔不會產生衝突

  // 修改了原有的 errorlog 模式 , 現在已經可以避免 thread 造成損毀

  // 還剩 直接中止程式的問題 

// 2026/01/22 修正直接停止程式函式的使用

  // 因該分成兩種形式 : 1.只需跳過該檔案 2.需要用 stopflag 讓全部的多執行緒有序停止

  // 先 實作跳過該檔案的方式

  // 先修改原本的程式碼 將只需跳過該檔案的部份進行修改

  // 替換完成

  // 目前的情況不會遇到 狀況2 , 需要等到有大量檔案時才需要加入

  // 第一版 多執行緒系統完成

// 2026/01/27 先計畫好如何實作進度條概念

  // 新增一個類別 ProgressObserver 只負責接收 進度的資訊,並決定如何呈現

  // 新增類別以 參考的方式傳入執行用的類別

  // 新增函式 以知道任務總數

  // 新增接收目前已執行任務數的功能

  // 因應多執行緒的使用,執行任務的計數因該使用 原子特性版的

  // 已經將任務計數加入流程當中 現在問題是該如何呈現

  // 確定流程 : 將決定如何呈現的方法與時機都交由 ProgressObserver 決定

  // 測試看看是否能正常執行

  // 可正常執行

  // 之後的進度條進階加工等到 GUI　階段在一起執行

　// 探討 日誌系統的實現與作用

  // 想要繼續升級作品的話,新增 日誌系統有其必要性

  // 原本的錯誤系統負擔過多的責任: 管理錯誤 + 寫錯誤日誌 因該將其拆解

  // 日誌系統的計劃:

     // 1.新增 log系統(正常運作的資訊),並確保其正常

     // 2.複製原本 errorlog 中的功能 放入 log 系統  並確認是否能正常運作

     // 3.移除 error 系統中的 errorlog 功能 

  // 先建立一個最小單位的日誌系統

  // 架設了一個最基礎的類別

  // 先實驗最小的開關動作

  // 排除問題

  // 基礎開關測試成功

// 2026/01/28 繼續實作 log 系統

  // 完成輸入資訊的函式實作

  // 函式完成進行實測

  // 先進行最小測試

  // 最小測試成功

  // 幾個基礎防呆測試

  // 測試多執行緒保護

  // 測試都通過

  // 已經具備基礎功能,嘗試整合進原本的流程再依據反饋進行修改

  // 在不修改原有程式的情況下,使用 log 紀錄 任務的執行

  // 成功完成 正確流程下的 log 紀錄

// 2026/01/29 對更多的檔案類型進行測試

  // 測試 UTF-8 編碼的檔案 :正常

  // 測試 UTF--16 編碼的檔案 :正常

  // 測試 單張圖片檔案 : 正常 , 可修改部份 : 圖片區塊的 log 系統紀錄缺少 

  // 目前需要將 log 以參考傳入 圖片處理類別函式中 以進行 log 紀錄

  // 修改完成 再次測試單張圖片的測試檔 : 成功

  // 對複數圖片的檔案進行測試 : 成功

  // 開始對文字清理函式導入 log 系統進行 紀錄

  // 導入成功

  // 對中文檔名的檔案進行預先處理以防出錯

  // 實作一個函式 確保輸入的路徑輸出後必定是 合規的 utf-8 std::string

  // 對中文檔名的檔案進行測試 : 成功

  // 對資料夾檔案進行測試

  // 輸出檔案正常 但是在 多執行緒的環境下 進度顯示條內的 std::wcout 會跟主要執行函式內的 std::wcout 互搶

  // 修改成具有鎖保護的輸出模式 : 成功

  // 可以開始 : 2.複製原本 errorlog 中的功能 放入 log 系統  並確認是否能正常運作

// 2026/01/30 開始將原本的功能轉移至 log 系統中

  // 最終的目標是把 errorlog 融入 log系統體系 , 此後就只有一份log 系統

  // 先解析原有的 errorlog 行動方式,方便後續複製

  // errorlog 的運作原理 : 
  
     // 1.主要使用 handle() 函式撰寫 errorlog 

     // 2.使用 handle() 函式必須給定專門的類別 ErrorInfo

     // 3.ErrorInfo 是一個專門容納許多 ErrorItem 的陣列     

     // 4.ErrorItem 有四個資訊 : level(錯誤等級),Category(錯誤發生區域),type(錯誤的大致類型),message(錯誤較詳細的描述)

     // 5.除了 message 每個資訊都是專門的enum , message 是 std::wstring

     // 6.每個 enum 都有實作 自己的 operator<< 以及 專門存放對應字串的 std::array 用來印出資訊到 errorlog 中

  // 因為 handle　是寫入ｅｒｒｏｒｌｏｇ的唯一入口，嘗試將ｌｏｇ系統放入　同步試跑看看

　// 目前的目標 : 以後 errorhandle 還是使用 handle() 來進行 errorlog 寫入,只是從 自己負責寫 變成 使用 log系統書寫

  // 先讓 errorhaandle 綁定 log系統

  // 從指標榜定改為使用 建構子綁定 確保使用 ErrorHandle 時一定有 log 系統

  // 在 errorhandle 中綁定了 log系統

  // 在 log 系統中 用函式多載 新增一個解讀 ErrorInfo 並進行寫入資訊的動作

  // 使用 原有的系統 加上 使用 std::string_view 低成本 的方式寫成全新的 log 字串

  // 已經完成新的 吃 errorInfo 的log() 函式

  // 產生一些錯誤的檔案去進行實測

  // 處裡臨時發現的狀況

  // 測試 detectorANSI 到底有沒有執行到

  // 確認偵測出來的資料型態符合預期,改為去查詢 偵測函式內部

  // 確認有跑道偵測,檢查偵測後的處理程序

  // 初步的小測試確認了 修改後的 ansi 偵測是正常運行的

  // 進階測試 

  // 進階測試成功已排除原有問題

// 2026/02/02 產生各類錯誤型態測試檔案

  // 先逐一進行測試

  // 目前皆無法順利找出錯誤點逐一修正錯誤

  // 錯誤1: ansi編碼無法順利抓到最標準的錯誤 \'81\'30  錯誤之 hex

  // 發現目前的程序根本無法跑到 第二 byte 偵測判斷式前

  // 發現目前執行任務的順序有重大問題 導致錯誤偵測完全不會起作用

  // 編碼錯誤偵測需要 正確的輸入才能有效作用需要調整現有程序

  // 需要將錯誤偵測的'流程'放入解碼中,這樣才能拿到能真正進行偵錯的資料

  // 只要是將負責的類別傳進去執行任務,責任就不會混淆

  // 先從 ansi 基底編碼開始嘗試,成功的話再將相同概念加入到 utf 體系中

  // 可以用函式多載的特性,保留做原始的只做解碼功能,跟新增一個擁有錯誤偵測跟處理的版本依據輸入參數不同做使用選擇

  // 先新增 使用 std::vector<uin8_t> 的函式驗證效果再去決定 之前函式的去留

  // 完成了改吃  std::vector<uin8_t> 做為參數的函式版本 + 所需的私有工具改版

// 2026/02/03 嘗試實際在解碼中使用新版的偵錯系統

  // 進行實測

  // 進行細節修改

  // 需將回傳值改為 std::optional<std::string> 以此來擁有安全表達回傳中止執行的功能

  // 再次測試

  // ANSI 版本正確中止並進行紀錄

  // 觀察 utf 體系的功用
  
  // 一樣是直接判段沒有處裡的 std::string , 高機率是無作用的

  // 用錯誤檔案進行測試

  // 先觀察原有的轉換方法

  // 先看一下原本處裡的輸出

　// 不加工的話 \uXXXX 跟 \'XX 都會被直接處理掉

  // 現代主要是看 \uXXXX , \'XX 是當時為了不支援的系統設計的

  // 需要將函式 修改為使用 std::vector<uin8_t> 的容器裝填 後 才能正確的進行錯誤偵測的動作

  // 新增了 std::vector<uint8_t> 版本的 utf 解碼器

  // 原本的流程為直接移除 \uc 控制符 並假設\'XX 數量為 2個(Big5 常態) , 將其調整為偵測並使用 控制符

  // 已經新增使用 \uc 控制符的功能

  // 可以開始嘗試加入錯誤偵測 與 錯誤系統

  // 在 utf-8 系統中新增 使用 std::vector<std::uint8_t> 的版本

  // 新增成功 嘗試完成 新版ｄｅｃｏｄｅ

　// 嘗試在原有流程中正確使用

  // 需要製作出真正穩定錯誤點的 utf8 的 rtf檔案

  // 改成在 文字清理階段在處裡 0x00 

  // 測試效果

  // 以證實 原本製造出的 錯誤測試檔案 實際沒有效果需要一個真的有效的改動

  // 原因是我把 單獨存在的 \'XX 直接忽略,但其實它在 正常檔案是正常存在的 不可忽略

  // 成功抓出錯誤檔案並正確 log + 中止程式

2026/02/04 關察 utf-16 轉換成 utf-8 的轉換與偵錯有無問題

  // 與之前一樣使用未經過加工的 std::string 是無法產生能被正確偵測錯誤的 byte 資料流的

  // 一樣先去看轉換的過程

  // 實務上的　ｒｔｆ　不存在用　ｕｔｆ１６　為基底編碼的檔案

　// 需要調整 關於 utf16 的路線

  // 第一步:將偵測結果為 utf16 的情況 修改為 用 log 紀錄 並改走 utf-8 的路線

  // 原有的 utf16 處裡 是基於 std::string 已經證實是無用的 將其移入保存庫,
     以後要支援在重寫使用 std::vector 的版本

  // 修改原有的介面使其能夠真正符合實際需求

  // 已經依照現有的實際需求修改完相關介面

  // 進行測試驗證修改後不影響 

  // 初步懷疑有檔案在 文字修飾階段被過多修飾

  // 進行驗證

  // 確認文字修飾階段有錯誤修飾的情況,嘗試解決

  // 用排除法找出是哪個函式

  // 需要修正函式原有的粗糙判斷來避免誤刪的情況

  // 初步修正成功需要細修以確保刪除乾淨

  // 調整完成已經乾淨了,進行進一步測試

  // 測試成功對更多檔案進行測試

  // 目前測試中 只剩 {\*\userprops} 這個群組無法清理乾淨

// 2026/02/05 先觀察其所在的整個群組分佈的狀態

  // 修改群組判斷條件為 {\\* 就成功了

  // {\\* 是最符合規範的 

  // 判斷條件給過多也不是好事

  // 已經可以正常修飾文字

  // 繼續之前的log 錯誤測試

  // 目前 圖片處理區的錯誤偵測無法抓到 大括號不平衡的問題

  // 需要為 群組括號偵測加入 新的判斷預防機制

// 2026/02/06 處裡圖片區塊的問題

  // 新增機制

  // 修改判斷式,避免空格或換行影響判斷

  // 測試是否能夠達到需求

  // 攔截失敗,需要調整判斷

  // 判斷為迴圈條件順序問題,調整後再次測試

  // 成功攔截 並且回傳後有正確紀錄 但是 未能正確停止 主流程

  // 修改回傳值為 enum 讓主流程可以依據回傳值 進行應對

  // 成功攔截 並且讓程式依需求終止

  // 測試第二個錯誤測試檔

  // 需新增抓出 過短 hex 數量的函式

  // 成功攔截並依照預設路徑進行處理

  // 測試下一個圖片錯誤測試檔案

  // 兩張圖片的錯誤檔案測試不符合預期,進行修改

  // 根據雙系統各自的特性,優先懷疑是迴圈的機制錯誤

  // while 迴圈的條件需要修改

  // 跟迴圈條件無關(但多加保險也沒關係),問題是計算出錯誤的 end 位置, 並且去對其進行動作導致的未定義行為

  // 先加入一個防範的機制

  // 沒有效果,驗證原本的推論

  // 原本的推論錯誤,實際上沒有超過字串大小,不該用不同平台的資訊(可能還是不同資訊)進行比較

  // 目前已知 範圍沒有問題,並且迴圈執行時是有更新位置的

  // 懷疑是其中一個 std::string_view 建立時給定的位置有問題

  // 確定是使用 std::string_view 的除錯流程干擾整體運行

  // 確定 是使用錯誤的參數建立 std::string_view , 導致使用期的函式觸發未定義行為, 已經成功修改.並使得函式恢復正常

  // 函式恢復正常,並且成功攔截製造之錯誤點,正確紀錄並切依照設計流程執行

  // 實作一個檢查函式在確保建立 std::string_view 時至少不要有 未定義行為

  // 該如何實作有點模糊

// 2026/02/09 完成此次 log 系統的 雙系統測試

  // 已經成功讓目前錯誤處裡檔案資料夾中的檔案都正確處理

  // 因為過程中有太多的錯誤系統不如預期的反應, 因此使用更多檔案來測試錯誤系統

  // 先做出所有 ansi 編碼常見的錯誤檔案

  // 目前的 錯誤檔案 : 加入\'81\'30 

  // 第二個 錯誤檔案 : 孤立的開頭 byte : 單獨加入 \'81 特意不給第二個byte

  // 第三個 錯誤檔案 : 孤立的合法結尾 byte : 單獨加 \'40 

  // 第四個  : \'XX 後面兩個 XX 不是 hex digit : 加入\'ZZ

  // 測試能否抓到新增的三個錯誤目標

  // 無法抓到 單獨存在的 開頭 byte

  // 需要先搞清楚其後方的 \cf0 \fs40 等控制符在容器裡的"樣子"

  // 被轉換函式 轉換成ＡＳＣＩＩ　碼了，新增一個判斷條件

　// 效果非常奇怪並且 看 log 會平繁的被錯誤觸發

  // 可能需要重構原本的 decode 方式

  // 需要將原本的作法 改成只對 \'XX 區塊做成

  // 轉換不需要更改,是檢查 需要正確的 byte 資料流

  // 修改原有的 轉換函式產生一個 乾淨的 純 \'XX byte 組成的陣列去檢查

  // 測試看看想法

  // 有成功抓到錯誤點,但需要探討一下是否足夠精準

  // 目前需求足夠了, 測試下一個檔案

  // 在遇到 \'ZZ\'G1 這種 不符合規定的 \'XX 的時候 目前的轉換完全不會處理 需要一個新的判斷函式

  // 實作新的函式

  // 偵錯成功,嘗試將解碼版本也修改為此版本

  // 目前的錯誤以成功偵測,需要處理的是後面 error系統改版後的是否放寬處裡問題　

// 2026/02/10 換成 utf-8 體系的錯誤檔案測試

  // 新增檔案 1 :非法前導位元: add 80 BF

  // 新增檔案 2 :缺少 continuation byte : add E4 B8  && add F0 9F 92

  // 新增檔案 3 :continuation byte 不合法 : add E4 28 AD

  // 新增檔案 4 :代理區  : add ED A0 80

  // 新增檔案 5 :超出 Unicode 上限

  // 新增檔案 6 :編碼混用

  // 測試 檔案1 : 成功 有抓到 非法 byte 開頭

  // 測試 檔案2 : 成功 有抓到 continuation byte 不合規定

  // 測試 檔案3 : 成功 有抓到 continuation byte 不合規定

  // 測試 檔案4 : 成功 有抓到 非正常區間

  // 測試 檔案5 : 成功 有抓到 超出上限

  // 測試 檔案6 : 成功 有抓到 非法 byte

  // utf8 錯誤偵測目前還算能用,換圖片區進行測試

  // 目前的錯誤測試檔案 : 在中間 加入控制符 : 有被成功攔截但是不是預期的 log 

  // 需要修整原本用來抓取結尾的函式,並在調整後加入 hex 區塊檢查的新功能

  // 將原本的回傳值改為 自訂的類別,這樣子掃描一次就可以得到需要用到的資訊

  // 目前的調整最需要的關鍵是 不能只依靠 大括號平衡來判斷圖片群組的邊界,還需要加入別的機制來配合

  // 硬性的圖片群組條件 : 在判定 hex 區域已經損壞後 : 第二次遇到 \par 或 遇到 控制群組開頭 {\ 就視為丟失右大括號

  // 初步完成新的函式並且將其套入主流程中

  // 測試新的流程能否順利抓到 錯誤點

  // 成功抓到錯誤點,因為有修改函式對之前的錯誤測試檔案進行測試

  // 也都可以抓到之前程式的錯誤點

  // 正常檔案的測試也通過 : 初步完成函式改進

// 2026/02/11 基礎錯誤檔案測試完成,開始最後一步 : 移除原有的 errorlog 功能以後由 log 系統負責全部的資訊寫入

  // 先觀察原有的ｅｒｒｏｒｌｏｇ　流程

  // 已經移除原有的 errorlog 功能 , 進行測試是否能正確運轉

  // 錯誤測試檔案 : 測試成功 只剩下 log.txt 並且正常紀錄

  // 正常檔案 : 測試成功 沒有影響正常檔案運作

  // 移除 errorlog 之後 errorhandle 需重新定義並實作錯誤系統的功能

  // 保留 errorhandle 的功能 之後再遇到錯誤問題需要執行判斷的時候其會是負責判斷任務的主要工具

  // 目前的錯誤系統的工具

     // 1. Errorsystem : 定義錯誤(錯誤是什麼)

     // 2. ErrorHandle : 如何處理錯誤(遇到錯誤該怎麼辦)

     // 3. logsystem   : 輸出所有資訊(錯誤資訊也由其負責)

  // 把之前的雙系統 log 移除

  // 初步完成 log 系統

  // 在下一步前想先解決目前單個檔案過長的問題

  // 計劃 :嘗試將一個類別工具　拆出去　練練手

  // 先新增該類別名稱的 .h + .cpp 檔案

  // 第一個次切割嘗試成功

  // 嘗試將 整個 ErrorSystem 都切割出去 

// 2026/02/12 繼續切割嘗試

// 2026/02/23 繼續切割動作

  // 測試 ErrorSystem 的切割是否正確

  // 成功 將 ErrorSystem 切割出去,且程式正常運作

  // 擬定分割計劃

  // 因該先依據負責的部份界定好邊界去進行初步的拆除 , 之後再觀察拆出來的檔案是否有需要再次進行拆除

  // 先嘗試在拆看看,在看看會有啥問題

  // 拆分了 log 系統 嘗試是否成功

  // 正常運作

  // 決定下一個拆分的目標

  // 下一個目標 偵測 rtf 格式的偵測系列

  // 發現會卡在 共用的 enum 上,先將其打包成共用的.h檔

  // 已完成 .h檔

// 2026/02/24 完成 .cpp 檔

  // 已完成 .o 檔測試,可以開始嘗試是否會影響正常運作

  // 基礎測試正常,可進行轉移工作

  // 先拆 detector 區塊

  // 完成 .ｈ檔部份
　
　// 完成 .cpp檔 做成 .o 檔看看

  // 成功編譯,測試是否加入主程式並正常運作

  // 測試正常

  // 再拆 Decode 前需要先拆 ErrorHandle

  // 完成 .h

  // 完成 .cpp

  // 測試是否能正常融入

  // 測試通過

  // 進行拆解 Decode 

// 2026/02/25 完成 .h 檔

  // 完成 .cpp 檔

  // 測試移回去後是否正常

// 2026/02/26 繼續拆換成群組段落

  // 嘗試整合回主流程看看是否正常

  // 成功

  // 拆除 文字處裡的區塊時,需要先把工具拆出來

  // 驗證拆出工具後的保護作用

  // 驗證通過已將工具拆出可以給其他區塊使用

  // 因為 C++ 更新後的衝突 需要將 log 語句中的 u8 移除 

  // 移除後沒有導致亂碼可正確銜接

  // 拆出完成 嘗試整合回主流程

  // 測試通過

  // 換成拆分圖片區塊

  // 需要先將資料夾工具先拆出

  // 工具已移除並確認整合沒問題

// 2026/03/02 完成 .cpp 檔案

  // 已成功整合回原本的流程中

  // 將語意結構 分成 拆解 與 解讀 兩種檔案方便之後擴充

  // 先做 拆解的部份

  // 測試是否可正常運轉

  // 正常運轉
 
  // 接下來換成解讀

  // 測試是否能順利接回原本的主流程

  // 測試通過

  // 目標都已經拆出主要流程,稍微整理一下原有主流程

  // 成功瘦身,但是變出很多檔案先整理一下 

  // 用資料夾分類

  // 需要釐清 include 分類後的 .h 檔 跟 自動找到 .cpp 檔

  // 移動 ErrorSystem.h/.cpp

  // 編譯看看

  // 錯誤很多採用舊的絕對路徑的方法,等之後再看看是否需要升級成 CMake

  // 單獨檔案測試成功,開始搬運工作

  // 移動 log.h/.cpp

  // 全部 .h/.cpp 檔案移動完成,且可以正常運作

// 2026/03/03 找尋新的備份方法

  // 可以開始學習使用 Git 備份整個檔案

  // 先安裝 Git

  




　






　


 

  

 












  　
 

　







  







